---
title: P12–P15：对象边界、迪米特法则与组合复用：停止穿透对象图
description: 以“包含地址、配送、会员优惠和订单关系的结算领域”为贯穿案例，按视频顺序从朴素实现、需求失败到重构命名，渐进理解 最少知识、Tell, Don’t Ask、组合、依赖与关联。
publishDate: 2026-08-23
updatedDate: 2026-08-27
language: zh
---

这不是把 P12–P15 的定义拼成一篇词典，而是让一个系统连续经历几轮需求。我们从 **包含地址、配送、会员优惠和订单关系的结算领域** 的最直接实现出发；每当代码真的承受不住下一次变化，才引入一个新边界。

最终要回答的是：**怎样让对象自己回答问题，并避免继承组合爆炸？** 学完的验收标准也不是记住名词，而是能根据知识边界、所有权和独立变化轴选择委托或组合。

## 先看完整推导路线

| 阶段 | 原视频 | 要解决的问题 | 具体设计动作 | 保护的结果 |
| --- | --- | --- | --- | --- |
| 1 | P12 | 长长的 getter 链为什么会让调用方依赖整个对象图？ | 结算代码穿透 user.profile.address.city 读取对象图 | 让调用方只与直接朋友协作，把导航封装进边界对象 | 内部结构变化不再传播到远处 |
| 2 | P13 | 如何把“取数据再判断”重构为对对象发出有意义的命令？ | 调用方取出多个字段后在外部拼业务判断 | 把问题交给拥有数据和规则的对象回答 | 规则与数据保持一致，调用代码表达业务意图 |
| 3 | P14 | 两个独立变化轴为什么不该通过子类数量相乘来实现？ | 会员等级 × 配送方式通过子类组合导致类数量相乘 | 把折扣和运费建成两个策略对象并组合 | 两个维度可以分别增加，不需要组合子类 |
| 4 | P15 | 如何根据所有权和生命周期，区分代码中的六种类关系？ | 类图中的连线随意使用，代码也说不清谁拥有谁的生命周期 | 按临时使用、长期引用、整体部分、继承与接口实现区分六种关系 | 关系强度与所有权变得可见，复用选择不再只靠继承 |

这张表不是术语目录，而是一份修改记录。每一行都要能回答：变化从哪里来、边界放在哪里、什么结果不应该被碰。阅读正文时建议停在每个“新需求”之前，先自己写修改清单，再比较后面的重构。

## 本章总结：把每个 P 压缩成一张决策卡

### P12 · 迪米特法则（一）：不要穿透对象边界取数据

**问题**：结算代码穿透 user.profile.address.city 读取对象图。

**动作**：让调用方只与直接朋友协作，把导航封装进边界对象。

**验证**：内部结构变化不再传播到远处。

**代价**：DTO 是数据载体，合理读取字段不必强行套对象法则。

### P13 · 迪米特法则（二）：把问题交回拥有数据的对象

**问题**：调用方取出多个字段后在外部拼业务判断。

**动作**：把问题交给拥有数据和规则的对象回答。

**验证**：规则与数据保持一致，调用代码表达业务意图。

**代价**：避免把所有行为塞进实体，跨聚合流程仍属于应用服务。

### P14 · 合成复用（一）：优先组合，而不是继承排列组合

**问题**：会员等级 × 配送方式通过子类组合导致类数量相乘。

**动作**：把折扣和运费建成两个策略对象并组合。

**验证**：两个维度可以分别增加，不需要组合子类。

**代价**：稳定且真正满足 is-a 的层次仍可使用继承。

### P15 · 合成复用（二）：看懂六种类关系与复用强度

**问题**：类图中的连线随意使用，代码也说不清谁拥有谁的生命周期。

**动作**：按临时使用、长期引用、整体部分、继承与接口实现区分六种关系。

**验证**：关系强度与所有权变得可见，复用选择不再只靠继承。

**代价**：UML 名称必须服务于设计沟通，不能取代代码中的明确所有权。

这些卡片故意把“代价”放在“验证”后面。先证明边界解决了真实问题，再讨论多了一层接口、注册表、包装器或组合根是否值得。没有代价说明的设计总结，通常只是模式宣传。

![对象边界、迪米特法则与组合复用：停止穿透对象图：同一案例随需求逐步演进](/images/notes/programming-thoughts/diagrams/chapter-object-boundaries-demeter-composition.svg)

## 起点：先写最直接、能工作的版本

这一版故意把决策和实现放在一起。它不是“错误代码”，而是需求尚少时合理的最低成本方案。请先运行它，并标出每个业务决定；接下来的 P12–P15 会连续修改同一案例。

```python
def quote(order) -> int:
    city = order.customer.profile.address.city
    level = order.customer.profile.membership.level
    shipping = 0 if city == "上海" else 12
    return int(sum(order.items) * (0.9 if level == "vip" else 1)) + shipping
```

## 第 1 轮需求：先解决问题，最后再叫它 最少知识（P12）

我们继续修改同一个“包含地址、配送、会员优惠和订单关系的结算领域”，不另起一个玩具项目。本轮的新压力是：**报价函数穿透 customer.profile.address 读取城市，依赖整棵对象图**。先不要背模式名，先列出要改的判断、对象、外部依赖和测试；这份清单就是“修改半径”。

这一轮真正要回答的是：长长的 getter 链为什么会让调用方依赖整个对象图？ 这是本章的第一轮，先保留最直接的实现，记录它面对下一次需求时会触碰哪些地方。

### 先记录修改半径

- 当前泄漏：结算代码穿透 user.profile.address.city 读取对象图。
- 设计动作：让调用方只与直接朋友协作，把导航封装进边界对象。
- 稳定结果：内部结构变化不再传播到远处。
- 仍需承担的代价：DTO 是数据载体，合理读取字段不必强行套对象法则。

如果四项里只有“新增一个类”而没有说明哪种变化被隔离，先不要合并这次重构。设计的证据不是角色数量，而是下一次同类需求能否只新增实现、只替换适配器或只调整组合根。

### 这一轮只做一个设计动作

本轮只落实 **迪米特法则（一）：不要穿透对象边界取数据**，围绕“最少知识、对象边界、导航链、封装”移动一个边界，不同时引入后续模式。把稳定流程写成调用方真正需要的业务语言；把供应商字段、创建细节或横切动作留在边界另一侧。

完成动作后再给它命名：这一轮对应 **最少知识、对象边界、导航链、封装**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

### Python 版本：只看本轮新增的协作关系

```python
from dataclasses import dataclass

@dataclass(frozen=True)
class Address:
    city: str

class Order:
    def __init__(self, address: Address) -> None:
        self._address = address

    def ships_to(self, city: str) -> bool:
        return self._address.city == city

order = Order(Address("上海"))
assert order.ships_to("上海")
```

读这段代码时只检查三件事：谁拥有规则、谁负责组合、谁可以被替换。不要因为示例用了 Protocol、注册表或装饰器，就默认生产代码也必须采用同样的层数；真正需要保留的是边界和行为契约。

### 用一个失败场景验证边界

先写一个会失败的测试，再写实现：新增一种“结算页读取收货地区”变化时，哪些稳定代码应该保持不动？如果实现抛出异常、返回空值或收到重复调用，调用方依赖的行为是否仍然成立？这一步能防止只验证 happy path。

本轮的验收标准是：长长的 getter 链为什么会让调用方依赖整个对象图？ 完成后，内部结构变化不再传播到远处。如果测试仍然必须启动所有外部服务，或仍要修改同一个大分支，说明这一步只换了名字，没有形成可验证的边界。

### 与前后两轮的连接

下一轮 P13 会继续检验这条边界：如果新增需求仍要修改本轮稳定代码，说明边界还没有放在真正的变化轴上。

不要把本轮结构当成永久答案。只要真实需求证明它没有减少修改范围，就应该回退一层，删除不必要的间接调用，再重新找变化轴。

## 第 2 轮需求：先解决问题，最后再叫它 Tell, Don’t Ask（P13）

我们继续修改同一个“包含地址、配送、会员优惠和订单关系的结算领域”，不另起一个玩具项目。本轮的新压力是：**配送资格判断散落在调用方，应交回拥有订单数据的对象**。先不要背模式名，先列出要改的判断、对象、外部依赖和测试；这份清单就是“修改半径”。

这一轮真正要回答的是：如何把“取数据再判断”重构为对对象发出有意义的命令？ 上一轮 P12 已经处理“最少知识”；这一轮不要把它推倒重来，而是只处理新的变化轴。

### 先记录修改半径

- 当前泄漏：调用方取出多个字段后在外部拼业务判断。
- 设计动作：把问题交给拥有数据和规则的对象回答。
- 稳定结果：规则与数据保持一致，调用代码表达业务意图。
- 仍需承担的代价：避免把所有行为塞进实体，跨聚合流程仍属于应用服务。

如果四项里只有“新增一个类”而没有说明哪种变化被隔离，先不要合并这次重构。设计的证据不是角色数量，而是下一次同类需求能否只新增实现、只替换适配器或只调整组合根。

### 这一轮只做一个设计动作

本轮只落实 **迪米特法则（二）：把问题交回拥有数据的对象**，围绕“Tell, Don’t Ask、委托、门面方法、封装变化”移动一个边界，不同时引入后续模式。把稳定流程写成调用方真正需要的业务语言；把供应商字段、创建细节或横切动作留在边界另一侧。

完成动作后再给它命名：这一轮对应 **Tell, Don’t Ask、委托、门面方法、封装变化**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

### Python 版本：只看本轮新增的协作关系

```python
from dataclasses import dataclass

@dataclass(frozen=True)
class DeliveryZone:
    cities: set[str]

class Order:
    def __init__(self, city: str, has_hazardous_goods: bool = False) -> None:
        self.city = city
        self.has_hazardous_goods = has_hazardous_goods

    def can_use_same_day_delivery(self, zone: DeliveryZone) -> bool:
        return self.city in zone.cities and not self.has_hazardous_goods

order = Order("上海")
if order.can_use_same_day_delivery(DeliveryZone({"上海"})):
    print("安排当天配送")
```

读这段代码时只检查三件事：谁拥有规则、谁负责组合、谁可以被替换。不要因为示例用了 Protocol、注册表或装饰器，就默认生产代码也必须采用同样的层数；真正需要保留的是边界和行为契约。

### 用一个失败场景验证边界

先写一个会失败的测试，再写实现：新增一种“订单配送资格判断”变化时，哪些稳定代码应该保持不动？如果实现抛出异常、返回空值或收到重复调用，调用方依赖的行为是否仍然成立？这一步能防止只验证 happy path。

本轮的验收标准是：如何把“取数据再判断”重构为对对象发出有意义的命令？ 完成后，规则与数据保持一致，调用代码表达业务意图。如果测试仍然必须启动所有外部服务，或仍要修改同一个大分支，说明这一步只换了名字，没有形成可验证的边界。

### 与前后两轮的连接

下一轮 P14 会继续检验这条边界：如果新增需求仍要修改本轮稳定代码，说明边界还没有放在真正的变化轴上。

不要把本轮结构当成永久答案。只要真实需求证明它没有减少修改范围，就应该回退一层，删除不必要的间接调用，再重新找变化轴。

## 第 3 轮需求：先解决问题，最后再叫它 组合（P14）

我们继续修改同一个“包含地址、配送、会员优惠和订单关系的结算领域”，不另起一个玩具项目。本轮的新压力是：**会员折扣与配送策略同时增长，继承会产生组合子类**。先不要背模式名，先列出要改的判断、对象、外部依赖和测试；这份清单就是“修改半径”。

这一轮真正要回答的是：两个独立变化轴为什么不该通过子类数量相乘来实现？ 上一轮 P13 已经处理“Tell, Don’t Ask”；这一轮不要把它推倒重来，而是只处理新的变化轴。

### 先记录修改半径

- 当前泄漏：会员等级 × 配送方式通过子类组合导致类数量相乘。
- 设计动作：把折扣和运费建成两个策略对象并组合。
- 稳定结果：两个维度可以分别增加，不需要组合子类。
- 仍需承担的代价：稳定且真正满足 is-a 的层次仍可使用继承。

如果四项里只有“新增一个类”而没有说明哪种变化被隔离，先不要合并这次重构。设计的证据不是角色数量，而是下一次同类需求能否只新增实现、只替换适配器或只调整组合根。

### 这一轮只做一个设计动作

本轮只落实 **合成复用（一）：优先组合，而不是继承排列组合**，围绕“组合、继承、委托、独立变化”移动一个边界，不同时引入后续模式。把稳定流程写成调用方真正需要的业务语言；把供应商字段、创建细节或横切动作留在边界另一侧。

完成动作后再给它命名：这一轮对应 **组合、继承、委托、独立变化**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

### Python 版本：只看本轮新增的协作关系

```python
from typing import Protocol

class DiscountPolicy(Protocol):
    def apply(self, price: int) -> int: ...

class ShippingPolicy(Protocol):
    def price(self, distance: int) -> int: ...

class OrderTotal:
    def __init__(self, shipping: ShippingPolicy, discount: DiscountPolicy) -> None:
        self.shipping = shipping
        self.discount = discount

    def total(self, distance: int, goods: int) -> int:
        return self.discount.apply(goods + self.shipping.price(distance))
```

读这段代码时只检查三件事：谁拥有规则、谁负责组合、谁可以被替换。不要因为示例用了 Protocol、注册表或装饰器，就默认生产代码也必须采用同样的层数；真正需要保留的是边界和行为契约。

### 用一个失败场景验证边界

先写一个会失败的测试，再写实现：新增一种“运费与会员优惠”变化时，哪些稳定代码应该保持不动？如果实现抛出异常、返回空值或收到重复调用，调用方依赖的行为是否仍然成立？这一步能防止只验证 happy path。

本轮的验收标准是：两个独立变化轴为什么不该通过子类数量相乘来实现？ 完成后，两个维度可以分别增加，不需要组合子类。如果测试仍然必须启动所有外部服务，或仍要修改同一个大分支，说明这一步只换了名字，没有形成可验证的边界。

### 与前后两轮的连接

下一轮 P15 会继续检验这条边界：如果新增需求仍要修改本轮稳定代码，说明边界还没有放在真正的变化轴上。

不要把本轮结构当成永久答案。只要真实需求证明它没有减少修改范围，就应该回退一层，删除不必要的间接调用，再重新找变化轴。

## 第 4 轮需求：先解决问题，最后再叫它 依赖与关联（P15）

我们继续修改同一个“包含地址、配送、会员优惠和订单关系的结算领域”，不另起一个玩具项目。本轮的新压力是：**订单、策略和支付端口的所有权与生命周期需要明确**。先不要背模式名，先列出要改的判断、对象、外部依赖和测试；这份清单就是“修改半径”。

这一轮真正要回答的是：如何根据所有权和生命周期，区分代码中的六种类关系？ 上一轮 P14 已经处理“组合”；这一轮不要把它推倒重来，而是只处理新的变化轴。

### 先记录修改半径

- 当前泄漏：类图中的连线随意使用，代码也说不清谁拥有谁的生命周期。
- 设计动作：按临时使用、长期引用、整体部分、继承与接口实现区分六种关系。
- 稳定结果：关系强度与所有权变得可见，复用选择不再只靠继承。
- 仍需承担的代价：UML 名称必须服务于设计沟通，不能取代代码中的明确所有权。

如果四项里只有“新增一个类”而没有说明哪种变化被隔离，先不要合并这次重构。设计的证据不是角色数量，而是下一次同类需求能否只新增实现、只替换适配器或只调整组合根。

### 这一轮只做一个设计动作

本轮只落实 **合成复用（二）：看懂六种类关系与复用强度**，围绕“依赖与关联、聚合与组合、泛化与实现、生命周期”移动一个边界，不同时引入后续模式。把稳定流程写成调用方真正需要的业务语言；把供应商字段、创建细节或横切动作留在边界另一侧。

完成动作后再给它命名：这一轮对应 **依赖与关联、聚合与组合、泛化与实现、生命周期**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

### Python 版本：只看本轮新增的协作关系

```python
from dataclasses import dataclass
from typing import Protocol

class PaymentPort(Protocol):
    def charge(self, cents: int) -> str: ...

@dataclass
class Order:
    customer_name: str
    items: list[int]

    def total(self) -> int:
        return sum(self.items)

def pay(order: Order, gateway: PaymentPort) -> str:
    return gateway.charge(order.total())
```

读这段代码时只检查三件事：谁拥有规则、谁负责组合、谁可以被替换。不要因为示例用了 Protocol、注册表或装饰器，就默认生产代码也必须采用同样的层数；真正需要保留的是边界和行为契约。

### 用一个失败场景验证边界

先写一个会失败的测试，再写实现：新增一种“订单领域对象关系”变化时，哪些稳定代码应该保持不动？如果实现抛出异常、返回空值或收到重复调用，调用方依赖的行为是否仍然成立？这一步能防止只验证 happy path。

本轮的验收标准是：如何根据所有权和生命周期，区分代码中的六种类关系？ 完成后，关系强度与所有权变得可见，复用选择不再只靠继承。如果测试仍然必须启动所有外部服务，或仍要修改同一个大分支，说明这一步只换了名字，没有形成可验证的边界。

### 与前后两轮的连接

这是本章最后一轮，接下来要把这条边界放回完整实现和 UML 中，检查它是否真的减少了修改半径。

不要把本轮结构当成永久答案。只要真实需求证明它没有减少修改范围，就应该回退一层，删除不必要的间接调用，再重新找变化轴。


## 演进完成后的 Python 实现

下面不是另一个示例，而是起点代码承受完上述需求后的版本。每个新增角色都能追溯到上面的某一轮真实修改压力；删掉任何一层，都应该能说出哪项需求会重新变难。阅读顺序建议是：先看依赖方向，再看对象拥有谁，最后看异常和替换测试。

```python
from dataclasses import dataclass
from typing import Protocol

class Shipping(Protocol):
    def price(self, city: str) -> int: ...

class Discount(Protocol):
    def apply(self, subtotal: int) -> int: ...

@dataclass(frozen=True)
class Order:
    city: str
    prices: list[int]

    def quote(self, shipping: Shipping, discount: Discount) -> int:
        return discount.apply(sum(self.prices)) + shipping.price(self.city)
```

![对象边界、迪米特法则与组合复用：停止穿透对象图：案例 UML 结构与对象关系](/images/notes/programming-thoughts/diagrams/chapter-object-boundaries-demeter-composition-uml.svg)

这张 UML 只画最终需要长期沟通的关系。虚线强调依赖或接口实现，菱形表示对象拥有或包装另一个对象；创建和调用细节仍以 Python 代码为准。

## 用真实业务结果验证，而不是测试模式名

测试不关心类图里有几个角色，只验证稳定业务结果、替换能力和关键失败边界。下面代码与上一个代码块拼接后可直接由 Python 3.12 执行。

```python
class LocalShipping:
    def price(self, city: str) -> int: return 0 if city == "上海" else 12
class Vip:
    def apply(self, subtotal: int) -> int: return round(subtotal * 0.9)

assert Order("上海", [100]).quote(LocalShipping(), Vip()) == 90
assert Order("北京", [100]).quote(LocalShipping(), Vip()) == 102
```

![对象边界、迪米特法则与组合复用：停止穿透对象图：同一份代码重构前后结构对照](/images/notes/programming-thoughts/diagrams/chapter-object-boundaries-demeter-composition-comparison.svg)

## 把连续重构放回一张决策表

| 检查项 | 仍然简单时 | 变化已经重复时 | 抽象后的验证 |
| --- | --- | --- | --- |
| 修改范围 | 一个分支或一个函数 | 同类判断散落到多个模块 | 新需求主要新增实现 |
| 依赖语言 | 具体类名与外部字段 | 业务规则被实现细节牵着走 | 调用方只看自己的业务能力 |
| 测试方式 | 端到端手工验证 | 每次都要启动整套环境 | 稳定规则可用替身局部测试 |
| 撤销成本 | 几乎没有 | 继续堆分支的成本持续上升 | 抽象仍能被一次小重构移除 |

这四行里最容易被忽略的是“撤销成本”。好的演进式设计不是层数最多，而是每一步都能解释、能验证、也能退回去。没有第二个真实用例时，保留直接实现通常更诚实；出现重复变化后仍拒绝命名边界，则会把复杂度转移给未来维护者。

## 一次完整复盘：从需求到代码审查

提交一次重构时，可以按下面顺序写审查说明：

1. 变化由谁发起？是业务规则、供应商协议、对象构造、运行时策略，还是横切关注点？
2. 这次修改触碰了哪些文件、分支和测试？重构后修改半径缩短了吗？
3. 新边界暴露的是业务语言，还是把旧实现的字段名换了一个包装？
4. 替换实现时，前置条件、返回值、异常和副作用是否仍然符合契约？
5. 如果未来发现抽象没有收益，能否只撤销本轮，而不牵动整个系统？

把这五问写进 Code Review，比在类名上争论“这是不是标准模式”更有用。它们也能帮助你把视频里的抽象迁移到完全不同的业务场景。

## 为什么这些分 P 必须连在一起读

单独看每个名词，很容易把设计理解成一组互不相关的技巧。放回同一条演进链后会发现，前一步通常在制造后一步所需的条件：先把变化聚到局部，才看得见稳定边界；先让依赖显式，才谈得上替换；先定义替换契约，才有资格把实现交给工厂、注册表或组合根。

同样，后一种结构并不会淘汰前一种。一个系统完全可能在入口保留简单分支，在核心流程使用协议，在基础设施侧使用注册表。选择标准不是“哪个模式更高级”，而是当前变化由谁发起、发生频率多高、失败后需要谁负责。

可以用三次追问检查理解是否连贯。第一，去掉新抽象后，哪一个真实需求会重新变难？第二，新增一种实现时，哪些稳定代码仍然会被修改？第三，实现抛错、超时或返回边界值时，调用方依赖的行为是否仍然成立？如果答不出来，先回到上一个更简单的版本。

本文把代码拆成多个小切片，是为了让每次决策清楚，并不建议在项目中为每个概念各建一层。真正落地时，应该把相邻且同向变化的角色合并，把仅服务一个调用点的抽象留在模块内部，只把跨模块需要共同遵守的契约公开出去。这样既保留演进路径，也不会把教学结构原样搬成生产结构。

## 容易走偏的地方

1. **第一版就设计最终形态。** 这会跳过最重要的证据收集，也让团队无法解释每层抽象来自哪个需求。
2. **用类的数量衡量设计。** 类少不等于简单，类多也不等于解耦；真正要看的是一次变化传播多远。
3. **只画重构后的图。** 没有“之前为什么痛”，读者只能背结构，无法迁移到新的问题。
4. **把 Protocol 当作自动解耦。** 如果接口仍然复制供应商语言，业务只是隔着一层继续依赖供应商。
5. **忽略运行时与失败语义。** 能正常返回只是契约的一部分，超时、重复、并发和部分失败同样决定实现能否替换。

## 什么时候停在更简单的版本

如果实现只有一个、变化方向长期稳定、团队无法给出第二个真实需求，先停在函数或小类即可。本文的每次重构都有前提：旧结构已经让某种变化重复付费。没有这个前提，所谓扩展性很容易变成需要长期维护的猜测。

反过来，如果同一个条件分支已经复制三次、测试必须连接真实基础设施、或一个调用方需要穿过三层对象才能回答业务问题，就不要再用“以后再说”掩盖结构债务。此时先写修改清单，再选择本文最小的一步，而不是一次引入所有模式。

## 练习：把视频推导重新走一遍

为“包含地址、配送、会员优惠和订单关系的结算领域”增加一个与现有实现明显不同的需求。第一轮禁止增加接口，只修改朴素版本并记录触碰点；第二轮使用本文对应步骤重构；第三轮再加入一个变化，验证旧流程是否保持不动。

提交物包括：重构前后的修改清单、一张依赖方向图、至少两个业务测试、一个共享契约测试，以及一段不超过 150 字的代价说明。最后回看 P12–P15，确认每个结构都能追溯到一个真实失败，而不是追溯到一个想背下来的名词。

## 小结

本章真正形成的是一条推理链：直接实现 → 新需求暴露耦合 → 找到变化轴 → 放置最小边界 → 用测试证明 → 写下代价。做到这一步，最少知识、Tell, Don’t Ask、组合、依赖与关联 才不再是彼此分散的知识点，而是同一段代码演进过程中自然出现的工具。
