---
title: P1–P4：变化、内聚、耦合与单一职责：从一团代码拆出清晰边界
description: 以“不断增加促销、支付与通知需求的电商结算服务”为贯穿案例，按视频顺序从朴素实现、需求失败到重构命名，渐进理解 变化成本、内聚度、耦合类型、SRP。
publishDate: 2026-08-23
updatedDate: 2026-08-27
language: zh
---

这不是把 P1–P4 的定义拼成一篇词典，而是让一个系统连续经历几轮需求。我们从 **不断增加促销、支付与通知需求的电商结算服务** 的最直接实现出发；每当代码真的承受不住下一次变化，才引入一个新边界。

最终要回答的是：**怎样从一次次难改的需求中，找出真正应该拆开的变化方向？** 学完的验收标准也不是记住名词，而是能用变化原因而不是代码行数判断模块边界。

## 先看完整推导路线

| 阶段 | 原视频 | 要解决的问题 | 具体设计动作 | 保护的结果 |
| --- | --- | --- | --- | --- |
| 1 | P1 | 代码“能运行”之后，怎样让下一次需求变化仍然容易？ | 业务规则、数据库和第三方 SDK 混在一个函数里 | 先识别会独立变化的方向，再给它们命名并划边界 | 修改被限制在一个局部，测试可以替换边界实现 |
| 2 | P2 | 一个模块里的成员为什么应该围绕同一个目标协作？ | 一个 OrderService 同时定价、发券、写库和发消息 | 把围绕同一业务目标的数据与行为聚到小模块 | 阅读代码时不必在多个无关概念之间来回跳转 |
| 3 | P3 | 模块必须协作时，怎样避免了解彼此太多细节？ | 业务层直接使用支付 SDK 的请求对象和错误码 | 定义业务所需的 PaymentPort，让适配器翻译外部协议 | 更换供应商时核心结算流程保持不动 |
| 4 | P4 | “只做一件事”为什么不是按代码行数拆类？ | ReportService 既取数、排版、保存又发送邮件 | 按照独立变化原因拆成数据查询、渲染和交付协作者 | 格式或发送渠道变化时不再碰同一大类 |

这张表不是术语目录，而是一份修改记录。每一行都要能回答：变化从哪里来、边界放在哪里、什么结果不应该被碰。阅读正文时建议停在每个“新需求”之前，先自己写修改清单，再比较后面的重构。

## 本章总结：把每个 P 压缩成一张决策卡

### P1 · 编程思想：先控制变化，再选择模式

**问题**：业务规则、数据库和第三方 SDK 混在一个函数里。

**动作**：先识别会独立变化的方向，再给它们命名并划边界。

**验证**：修改被限制在一个局部，测试可以替换边界实现。

**代价**：抽象本身也有维护成本，不要为想象中的变化建框架。

### P2 · 高内聚：让一起变化的代码待在一起

**问题**：一个 OrderService 同时定价、发券、写库和发消息。

**动作**：把围绕同一业务目标的数据与行为聚到小模块。

**验证**：阅读代码时不必在多个无关概念之间来回跳转。

**代价**：高内聚不是文件越小越好，而是变化原因一致。

### P3 · 低耦合：让协作依赖稳定接口

**问题**：业务层直接使用支付 SDK 的请求对象和错误码。

**动作**：定义业务所需的 PaymentPort，让适配器翻译外部协议。

**验证**：更换供应商时核心结算流程保持不动。

**代价**：接口过细会制造调用噪音，边界应围绕完整能力。

### P4 · 单一职责：一个模块只对一种变化负责

**问题**：ReportService 既取数、排版、保存又发送邮件。

**动作**：按照独立变化原因拆成数据查询、渲染和交付协作者。

**验证**：格式或发送渠道变化时不再碰同一大类。

**代价**：不要按每个方法机械拆类，先找真实的责任主体。

这些卡片故意把“代价”放在“验证”后面。先证明边界解决了真实问题，再讨论多了一层接口、注册表、包装器或组合根是否值得。没有代价说明的设计总结，通常只是模式宣传。

![变化、内聚、耦合与单一职责：从一团代码拆出清晰边界：同一案例随需求逐步演进](/images/notes/programming-thoughts/diagrams/chapter-change-cohesion-coupling-srp.svg)

## 起点：先写最直接、能工作的版本

这一版故意把决策和实现放在一起。它不是“错误代码”，而是需求尚少时合理的最低成本方案。请先运行它，并标出每个业务决定；接下来的 P1–P4 会连续修改同一案例。

```python
def checkout(order, payment_sdk):
    subtotal = sum(item["price"] for item in order["items"])
    total = round(subtotal * (0.9 if order["vip"] else 1))
    trade = payment_sdk.pay(order["id"], total)
    print(f"paid:{trade}")
    return total
```

## 第 1 轮需求：先解决问题，最后再叫它 变化成本（P1）

我们继续修改同一个“不断增加促销、支付与通知需求的电商结算服务”，不另起一个玩具项目。本轮的新压力是：**结算、折扣、支付和通知全挤在 checkout() 中，一次活动改动会碰完整流程**。先不要背模式名，先列出要改的判断、对象、外部依赖和测试；这份清单就是“修改半径”。

这一轮真正要回答的是：代码“能运行”之后，怎样让下一次需求变化仍然容易？ 这是本章的第一轮，先保留最直接的实现，记录它面对下一次需求时会触碰哪些地方。

### 先记录修改半径

- 当前泄漏：业务规则、数据库和第三方 SDK 混在一个函数里。
- 设计动作：先识别会独立变化的方向，再给它们命名并划边界。
- 稳定结果：修改被限制在一个局部，测试可以替换边界实现。
- 仍需承担的代价：抽象本身也有维护成本，不要为想象中的变化建框架。

如果四项里只有“新增一个类”而没有说明哪种变化被隔离，先不要合并这次重构。设计的证据不是角色数量，而是下一次同类需求能否只新增实现、只替换适配器或只调整组合根。

### 这一轮只做一个设计动作

本轮只落实 **编程思想：先控制变化，再选择模式**，围绕“变化成本、抽象、边界、可测试性”移动一个边界，不同时引入后续模式。把稳定流程写成调用方真正需要的业务语言；把供应商字段、创建细节或横切动作留在边界另一侧。

完成动作后再给它命名：这一轮对应 **变化成本、抽象、边界、可测试性**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

### Python 版本：只看本轮新增的协作关系

```python
from typing import Protocol

class Notifier(Protocol):
    def send(self, message: str) -> None: ...

class ConsoleNotifier:
    def send(self, message: str) -> None:
        print(message)

class WelcomeUser:
    def __init__(self, notifier: Notifier) -> None:
        self.notifier = notifier

    def run(self, name: str) -> None:
        self.notifier.send(f"欢迎 {name}")

WelcomeUser(ConsoleNotifier()).run("小明")
```

读这段代码时只检查三件事：谁拥有规则、谁负责组合、谁可以被替换。不要因为示例用了 Protocol、注册表或装饰器，就默认生产代码也必须采用同样的层数；真正需要保留的是边界和行为契约。

### 用一个失败场景验证边界

先写一个会失败的测试，再写实现：新增一种“可插拔通知服务”变化时，哪些稳定代码应该保持不动？如果实现抛出异常、返回空值或收到重复调用，调用方依赖的行为是否仍然成立？这一步能防止只验证 happy path。

本轮的验收标准是：代码“能运行”之后，怎样让下一次需求变化仍然容易？ 完成后，修改被限制在一个局部，测试可以替换边界实现。如果测试仍然必须启动所有外部服务，或仍要修改同一个大分支，说明这一步只换了名字，没有形成可验证的边界。

### 与前后两轮的连接

下一轮 P2 会继续检验这条边界：如果新增需求仍要修改本轮稳定代码，说明边界还没有放在真正的变化轴上。

不要把本轮结构当成永久答案。只要真实需求证明它没有减少修改范围，就应该回退一层，删除不必要的间接调用，再重新找变化轴。

## 第 2 轮需求：先解决问题，最后再叫它 内聚度（P2）

我们继续修改同一个“不断增加促销、支付与通知需求的电商结算服务”，不另起一个玩具项目。本轮的新压力是：**第二种折扣出现后，定价规则开始反复修改结算函数**。先不要背模式名，先列出要改的判断、对象、外部依赖和测试；这份清单就是“修改半径”。

这一轮真正要回答的是：一个模块里的成员为什么应该围绕同一个目标协作？ 上一轮 P1 已经处理“变化成本”；这一轮不要把它推倒重来，而是只处理新的变化轴。

### 先记录修改半径

- 当前泄漏：一个 OrderService 同时定价、发券、写库和发消息。
- 设计动作：把围绕同一业务目标的数据与行为聚到小模块。
- 稳定结果：阅读代码时不必在多个无关概念之间来回跳转。
- 仍需承担的代价：高内聚不是文件越小越好，而是变化原因一致。

如果四项里只有“新增一个类”而没有说明哪种变化被隔离，先不要合并这次重构。设计的证据不是角色数量，而是下一次同类需求能否只新增实现、只替换适配器或只调整组合根。

### 这一轮只做一个设计动作

本轮只落实 **高内聚：让一起变化的代码待在一起**，围绕“内聚度、模块职责、局部推理、命名”移动一个边界，不同时引入后续模式。把稳定流程写成调用方真正需要的业务语言；把供应商字段、创建细节或横切动作留在边界另一侧。

完成动作后再给它命名：这一轮对应 **内聚度、模块职责、局部推理、命名**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

### Python 版本：只看本轮新增的协作关系

```python
from dataclasses import dataclass
from typing import Protocol

@dataclass(frozen=True)
class LineItem:
    name: str
    price: int

class DiscountPolicy(Protocol):
    def apply(self, subtotal: int) -> int: ...

class VipDiscount:
    def apply(self, subtotal: int) -> int:
        return round(subtotal * 0.9)

def total(items: list[LineItem], policy: DiscountPolicy) -> int:
    return policy.apply(sum(item.price for item in items))

print(total([LineItem("书", 100)], VipDiscount()))
```

读这段代码时只检查三件事：谁拥有规则、谁负责组合、谁可以被替换。不要因为示例用了 Protocol、注册表或装饰器，就默认生产代码也必须采用同样的层数；真正需要保留的是边界和行为契约。

### 用一个失败场景验证边界

先写一个会失败的测试，再写实现：新增一种“订单定价模块”变化时，哪些稳定代码应该保持不动？如果实现抛出异常、返回空值或收到重复调用，调用方依赖的行为是否仍然成立？这一步能防止只验证 happy path。

本轮的验收标准是：一个模块里的成员为什么应该围绕同一个目标协作？ 完成后，阅读代码时不必在多个无关概念之间来回跳转。如果测试仍然必须启动所有外部服务，或仍要修改同一个大分支，说明这一步只换了名字，没有形成可验证的边界。

### 与前后两轮的连接

下一轮 P3 会继续检验这条边界：如果新增需求仍要修改本轮稳定代码，说明边界还没有放在真正的变化轴上。

不要把本轮结构当成永久答案。只要真实需求证明它没有减少修改范围，就应该回退一层，删除不必要的间接调用，再重新找变化轴。

## 第 3 轮需求：先解决问题，最后再叫它 耦合类型（P3）

我们继续修改同一个“不断增加促销、支付与通知需求的电商结算服务”，不另起一个玩具项目。本轮的新压力是：**支付供应商更换后，SDK 字段与错误码渗入业务流程**。先不要背模式名，先列出要改的判断、对象、外部依赖和测试；这份清单就是“修改半径”。

这一轮真正要回答的是：模块必须协作时，怎样避免了解彼此太多细节？ 上一轮 P2 已经处理“内聚度”；这一轮不要把它推倒重来，而是只处理新的变化轴。

### 先记录修改半径

- 当前泄漏：业务层直接使用支付 SDK 的请求对象和错误码。
- 设计动作：定义业务所需的 PaymentPort，让适配器翻译外部协议。
- 稳定结果：更换供应商时核心结算流程保持不动。
- 仍需承担的代价：接口过细会制造调用噪音，边界应围绕完整能力。

如果四项里只有“新增一个类”而没有说明哪种变化被隔离，先不要合并这次重构。设计的证据不是角色数量，而是下一次同类需求能否只新增实现、只替换适配器或只调整组合根。

### 这一轮只做一个设计动作

本轮只落实 **低耦合：让协作依赖稳定接口**，围绕“耦合类型、接口、端口、替换成本”移动一个边界，不同时引入后续模式。把稳定流程写成调用方真正需要的业务语言；把供应商字段、创建细节或横切动作留在边界另一侧。

完成动作后再给它命名：这一轮对应 **耦合类型、接口、端口、替换成本**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

### Python 版本：只看本轮新增的协作关系

```python
from typing import Protocol

class PaymentPort(Protocol):
    def charge(self, order_id: str, cents: int) -> str: ...

class VendorSdk:
    def pay(self, *, ref: str, amount: int) -> dict[str, str]:
        return {"trade_no": f"vendor-{ref}", "status": "paid"}

class VendorAdapter:
    def __init__(self, sdk: VendorSdk) -> None:
        self.sdk = sdk

    def charge(self, order_id: str, cents: int) -> str:
        result = self.sdk.pay(ref=order_id, amount=cents)
        return result["trade_no"]

receipt: PaymentPort = VendorAdapter(VendorSdk())
print(receipt.charge("order-1", 1999))
```

读这段代码时只检查三件事：谁拥有规则、谁负责组合、谁可以被替换。不要因为示例用了 Protocol、注册表或装饰器，就默认生产代码也必须采用同样的层数；真正需要保留的是边界和行为契约。

### 用一个失败场景验证边界

先写一个会失败的测试，再写实现：新增一种“第三方支付适配”变化时，哪些稳定代码应该保持不动？如果实现抛出异常、返回空值或收到重复调用，调用方依赖的行为是否仍然成立？这一步能防止只验证 happy path。

本轮的验收标准是：模块必须协作时，怎样避免了解彼此太多细节？ 完成后，更换供应商时核心结算流程保持不动。如果测试仍然必须启动所有外部服务，或仍要修改同一个大分支，说明这一步只换了名字，没有形成可验证的边界。

### 与前后两轮的连接

下一轮 P4 会继续检验这条边界：如果新增需求仍要修改本轮稳定代码，说明边界还没有放在真正的变化轴上。

不要把本轮结构当成永久答案。只要真实需求证明它没有减少修改范围，就应该回退一层，删除不必要的间接调用，再重新找变化轴。

## 第 4 轮需求：先解决问题，最后再叫它 SRP（P4）

我们继续修改同一个“不断增加促销、支付与通知需求的电商结算服务”，不另起一个玩具项目。本轮的新压力是：**邮件通知再次变化，结算用例需要只保留编排职责**。先不要背模式名，先列出要改的判断、对象、外部依赖和测试；这份清单就是“修改半径”。

这一轮真正要回答的是：“只做一件事”为什么不是按代码行数拆类？ 上一轮 P3 已经处理“耦合类型”；这一轮不要把它推倒重来，而是只处理新的变化轴。

### 先记录修改半径

- 当前泄漏：ReportService 既取数、排版、保存又发送邮件。
- 设计动作：按照独立变化原因拆成数据查询、渲染和交付协作者。
- 稳定结果：格式或发送渠道变化时不再碰同一大类。
- 仍需承担的代价：不要按每个方法机械拆类，先找真实的责任主体。

如果四项里只有“新增一个类”而没有说明哪种变化被隔离，先不要合并这次重构。设计的证据不是角色数量，而是下一次同类需求能否只新增实现、只替换适配器或只调整组合根。

### 这一轮只做一个设计动作

本轮只落实 **单一职责：一个模块只对一种变化负责**，围绕“SRP、变化原因、职责拆分、协作者”移动一个边界，不同时引入后续模式。把稳定流程写成调用方真正需要的业务语言；把供应商字段、创建细节或横切动作留在边界另一侧。

完成动作后再给它命名：这一轮对应 **SRP、变化原因、职责拆分、协作者**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

### Python 版本：只看本轮新增的协作关系

```python
from dataclasses import dataclass

@dataclass(frozen=True)
class Report:
    rows: list[dict[str, str]]

def load_report() -> Report:
    return Report([{"name": "Ada", "score": "100"}])

def render_markdown(report: Report) -> str:
    return "\n".join(f"- {row['name']}: {row['score']}" for row in report.rows)

def save_file(text: str, path: str) -> None:
    print(f"保存到 {path}:\n{text}")

def publish_report() -> None:
    save_file(render_markdown(load_report()), "report.md")

publish_report()
```

读这段代码时只检查三件事：谁拥有规则、谁负责组合、谁可以被替换。不要因为示例用了 Protocol、注册表或装饰器，就默认生产代码也必须采用同样的层数；真正需要保留的是边界和行为契约。

### 用一个失败场景验证边界

先写一个会失败的测试，再写实现：新增一种“报表生成与发送”变化时，哪些稳定代码应该保持不动？如果实现抛出异常、返回空值或收到重复调用，调用方依赖的行为是否仍然成立？这一步能防止只验证 happy path。

本轮的验收标准是：“只做一件事”为什么不是按代码行数拆类？ 完成后，格式或发送渠道变化时不再碰同一大类。如果测试仍然必须启动所有外部服务，或仍要修改同一个大分支，说明这一步只换了名字，没有形成可验证的边界。

### 与前后两轮的连接

这是本章最后一轮，接下来要把这条边界放回完整实现和 UML 中，检查它是否真的减少了修改半径。

不要把本轮结构当成永久答案。只要真实需求证明它没有减少修改范围，就应该回退一层，删除不必要的间接调用，再重新找变化轴。


## 演进完成后的 Python 实现

下面不是另一个示例，而是起点代码承受完上述需求后的版本。每个新增角色都能追溯到上面的某一轮真实修改压力；删掉任何一层，都应该能说出哪项需求会重新变难。阅读顺序建议是：先看依赖方向，再看对象拥有谁，最后看异常和替换测试。

```python
from dataclasses import dataclass
from typing import Protocol

class Discount(Protocol):
    def apply(self, subtotal: int) -> int: ...

class Payment(Protocol):
    def charge(self, order_id: str, cents: int) -> str: ...

@dataclass(frozen=True)
class Order:
    id: str
    prices: list[int]

class Checkout:
    def __init__(self, discount: Discount, payment: Payment) -> None:
        self.discount, self.payment = discount, payment

    def run(self, order: Order) -> tuple[int, str]:
        total = self.discount.apply(sum(order.prices))
        return total, self.payment.charge(order.id, total)
```

![变化、内聚、耦合与单一职责：从一团代码拆出清晰边界：案例 UML 结构与对象关系](/images/notes/programming-thoughts/diagrams/chapter-change-cohesion-coupling-srp-uml.svg)

这张 UML 只画最终需要长期沟通的关系。虚线强调依赖或接口实现，菱形表示对象拥有或包装另一个对象；创建和调用细节仍以 Python 代码为准。

## 用真实业务结果验证，而不是测试模式名

测试不关心类图里有几个角色，只验证稳定业务结果、替换能力和关键失败边界。下面代码与上一个代码块拼接后可直接由 Python 3.12 执行。

```python
class VipDiscount:
    def apply(self, subtotal: int) -> int: return round(subtotal * 0.9)

class FakePayment:
    def charge(self, order_id: str, cents: int) -> str: return f"fake-{order_id}-{cents}"

assert Checkout(VipDiscount(), FakePayment()).run(Order("o1", [80, 20])) == (90, "fake-o1-90")
```

![变化、内聚、耦合与单一职责：从一团代码拆出清晰边界：同一份代码重构前后结构对照](/images/notes/programming-thoughts/diagrams/chapter-change-cohesion-coupling-srp-comparison.svg)

## 把连续重构放回一张决策表

| 检查项 | 仍然简单时 | 变化已经重复时 | 抽象后的验证 |
| --- | --- | --- | --- |
| 修改范围 | 一个分支或一个函数 | 同类判断散落到多个模块 | 新需求主要新增实现 |
| 依赖语言 | 具体类名与外部字段 | 业务规则被实现细节牵着走 | 调用方只看自己的业务能力 |
| 测试方式 | 端到端手工验证 | 每次都要启动整套环境 | 稳定规则可用替身局部测试 |
| 撤销成本 | 几乎没有 | 继续堆分支的成本持续上升 | 抽象仍能被一次小重构移除 |

这四行里最容易被忽略的是“撤销成本”。好的演进式设计不是层数最多，而是每一步都能解释、能验证、也能退回去。没有第二个真实用例时，保留直接实现通常更诚实；出现重复变化后仍拒绝命名边界，则会把复杂度转移给未来维护者。

## 一次完整复盘：从需求到代码审查

提交一次重构时，可以按下面顺序写审查说明：

1. 变化由谁发起？是业务规则、供应商协议、对象构造、运行时策略，还是横切关注点？
2. 这次修改触碰了哪些文件、分支和测试？重构后修改半径缩短了吗？
3. 新边界暴露的是业务语言，还是把旧实现的字段名换了一个包装？
4. 替换实现时，前置条件、返回值、异常和副作用是否仍然符合契约？
5. 如果未来发现抽象没有收益，能否只撤销本轮，而不牵动整个系统？

把这五问写进 Code Review，比在类名上争论“这是不是标准模式”更有用。它们也能帮助你把视频里的抽象迁移到完全不同的业务场景。

## 为什么这些分 P 必须连在一起读

单独看每个名词，很容易把设计理解成一组互不相关的技巧。放回同一条演进链后会发现，前一步通常在制造后一步所需的条件：先把变化聚到局部，才看得见稳定边界；先让依赖显式，才谈得上替换；先定义替换契约，才有资格把实现交给工厂、注册表或组合根。

同样，后一种结构并不会淘汰前一种。一个系统完全可能在入口保留简单分支，在核心流程使用协议，在基础设施侧使用注册表。选择标准不是“哪个模式更高级”，而是当前变化由谁发起、发生频率多高、失败后需要谁负责。

可以用三次追问检查理解是否连贯。第一，去掉新抽象后，哪一个真实需求会重新变难？第二，新增一种实现时，哪些稳定代码仍然会被修改？第三，实现抛错、超时或返回边界值时，调用方依赖的行为是否仍然成立？如果答不出来，先回到上一个更简单的版本。

本文把代码拆成多个小切片，是为了让每次决策清楚，并不建议在项目中为每个概念各建一层。真正落地时，应该把相邻且同向变化的角色合并，把仅服务一个调用点的抽象留在模块内部，只把跨模块需要共同遵守的契约公开出去。这样既保留演进路径，也不会把教学结构原样搬成生产结构。

## 容易走偏的地方

1. **第一版就设计最终形态。** 这会跳过最重要的证据收集，也让团队无法解释每层抽象来自哪个需求。
2. **用类的数量衡量设计。** 类少不等于简单，类多也不等于解耦；真正要看的是一次变化传播多远。
3. **只画重构后的图。** 没有“之前为什么痛”，读者只能背结构，无法迁移到新的问题。
4. **把 Protocol 当作自动解耦。** 如果接口仍然复制供应商语言，业务只是隔着一层继续依赖供应商。
5. **忽略运行时与失败语义。** 能正常返回只是契约的一部分，超时、重复、并发和部分失败同样决定实现能否替换。

## 什么时候停在更简单的版本

如果实现只有一个、变化方向长期稳定、团队无法给出第二个真实需求，先停在函数或小类即可。本文的每次重构都有前提：旧结构已经让某种变化重复付费。没有这个前提，所谓扩展性很容易变成需要长期维护的猜测。

反过来，如果同一个条件分支已经复制三次、测试必须连接真实基础设施、或一个调用方需要穿过三层对象才能回答业务问题，就不要再用“以后再说”掩盖结构债务。此时先写修改清单，再选择本文最小的一步，而不是一次引入所有模式。

## 练习：把视频推导重新走一遍

为“不断增加促销、支付与通知需求的电商结算服务”增加一个与现有实现明显不同的需求。第一轮禁止增加接口，只修改朴素版本并记录触碰点；第二轮使用本文对应步骤重构；第三轮再加入一个变化，验证旧流程是否保持不动。

提交物包括：重构前后的修改清单、一张依赖方向图、至少两个业务测试、一个共享契约测试，以及一段不超过 150 字的代价说明。最后回看 P1–P4，确认每个结构都能追溯到一个真实失败，而不是追溯到一个想背下来的名词。

## 小结

本章真正形成的是一条推理链：直接实现 → 新需求暴露耦合 → 找到变化轴 → 放置最小边界 → 用测试证明 → 写下代价。做到这一步，变化成本、内聚度、耦合类型、SRP 才不再是彼此分散的知识点，而是同一段代码演进过程中自然出现的工具。
