---
title: P45–P46：享元、状态分离与内存优化：先测量，再共享
description: 以“需要渲染百万格地图瓦片的游戏场景”为贯穿案例，按视频顺序从朴素实现、需求失败到重构命名，渐进理解 Flyweight、享元工厂。
publishDate: 2026-08-23
updatedDate: 2026-08-27
language: zh
---

这不是把 P45–P46 的定义拼成一篇词典，而是让一个系统连续经历几轮需求。我们从 **需要渲染百万格地图瓦片的游戏场景** 的最直接实现出发；每当代码真的承受不住下一次变化，才引入一个新边界。

最终要回答的是：**怎样分开可共享状态与上下文状态，并证明优化真的有效？** 学完的验收标准也不是记住名词，而是能通过不可变共享、缓存键和内存测量验证享元收益。

## 先看完整推导路线

| 阶段 | 原视频 | 要解决的问题 | 具体设计动作 | 保护的结果 |
| --- | --- | --- | --- | --- |
| 1 | P45 | 大量相似对象真正重复的是什么，哪些状态不能共享？ | 十万个字符对象各自保存重复字体、字号和颜色 | 共享不可变样式，把位置等上下文状态留在调用处 | 大量细粒度对象的重复内存下降 |
| 2 | P46 | 怎样证明对象共享真的节省内存，而不是凭感觉增加复杂度？ | 地图每个格子都创建完整纹理与碰撞配置 | 享元工厂按稳定键缓存不可变 TileType，坐标留给 Tile | 纹理和规则只保存一次，实例只保留外部状态 |

这张表不是术语目录，而是一份修改记录。每一行都要能回答：变化从哪里来、边界放在哪里、什么结果不应该被碰。阅读正文时建议停在每个“新需求”之前，先自己写修改清单，再比较后面的重构。

## 本章总结：把每个 P 压缩成一张决策卡

### P45 · 享元（一）：分离可共享状态与上下文状态

**问题**：十万个字符对象各自保存重复字体、字号和颜色。

**动作**：共享不可变样式，把位置等上下文状态留在调用处。

**验证**：大量细粒度对象的重复内存下降。

**代价**：可变状态一旦进入享元，会造成难以追踪的串扰。

### P46 · 享元（二）：用共享瓦片降低地图对象开销

**问题**：地图每个格子都创建完整纹理与碰撞配置。

**动作**：享元工厂按稳定键缓存不可变 TileType，坐标留给 Tile。

**验证**：纹理和规则只保存一次，实例只保留外部状态。

**代价**：先用内存剖析验证收益，低基数对象不值得引入缓存。

这些卡片故意把“代价”放在“验证”后面。先证明边界解决了真实问题，再讨论多了一层接口、注册表、包装器或组合根是否值得。没有代价说明的设计总结，通常只是模式宣传。

![享元、状态分离与内存优化：先测量，再共享：同一案例随需求逐步演进](/images/notes/programming-thoughts/diagrams/chapter-flyweight-state-memory.svg)

## 起点：先写最直接、能工作的版本

这一版故意把决策和实现放在一起。它不是“错误代码”，而是需求尚少时合理的最低成本方案。请先运行它，并标出每个业务决定；接下来的 P45–P46 会连续修改同一案例。

```python
tiles = [
    {"x": x, "y": y, "texture": load_texture("grass.png"), "walkable": True}
    for x in range(1000) for y in range(1000)
]
# 同一份纹理和规则被放进一百万个对象。
```

## 第 1 轮需求：先解决问题，最后再叫它 Flyweight（P45）

我们继续修改同一个“需要渲染百万格地图瓦片的游戏场景”，不另起一个玩具项目。本轮的新压力是：**百万瓦片各自保存同一纹理和通行规则，内存被重复状态占满**。先不要背模式名，先列出要改的判断、对象、外部依赖和测试；这份清单就是“修改半径”。

这一轮真正要回答的是：大量相似对象真正重复的是什么，哪些状态不能共享？ 这是本章的第一轮，先保留最直接的实现，记录它面对下一次需求时会触碰哪些地方。

### 先记录修改半径

- 当前泄漏：十万个字符对象各自保存重复字体、字号和颜色。
- 设计动作：共享不可变样式，把位置等上下文状态留在调用处。
- 稳定结果：大量细粒度对象的重复内存下降。
- 仍需承担的代价：可变状态一旦进入享元，会造成难以追踪的串扰。

如果四项里只有“新增一个类”而没有说明哪种变化被隔离，先不要合并这次重构。设计的证据不是角色数量，而是下一次同类需求能否只新增实现、只替换适配器或只调整组合根。

### 这一轮只做一个设计动作

本轮只落实 **享元（一）：分离可共享状态与上下文状态**，围绕“Flyweight、内蕴状态、外蕴状态、对象池”移动一个边界，不同时引入后续模式。把稳定流程写成调用方真正需要的业务语言；把供应商字段、创建细节或横切动作留在边界另一侧。

完成动作后再给它命名：这一轮对应 **Flyweight、内蕴状态、外蕴状态、对象池**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

### Python 版本：只看本轮新增的协作关系

```python
from dataclasses import dataclass

@dataclass(frozen=True)
class TextStyle:
    font: str
    size: int
    color: str

class StylePool:
    def __init__(self) -> None:
        self._styles: dict[tuple[str, int, str], TextStyle] = {}

    def get(self, font: str, size: int, color: str) -> TextStyle:
        key = (font, size, color)
        self._styles.setdefault(key, TextStyle(*key))
        return self._styles[key]

pool = StylePool()
print(pool.get("sans", 14, "black") is pool.get("sans", 14, "black"))
```

读这段代码时只检查三件事：谁拥有规则、谁负责组合、谁可以被替换。不要因为示例用了 Protocol、注册表或装饰器，就默认生产代码也必须采用同样的层数；真正需要保留的是边界和行为契约。

### 用一个失败场景验证边界

先写一个会失败的测试，再写实现：新增一种“文本字符样式”变化时，哪些稳定代码应该保持不动？如果实现抛出异常、返回空值或收到重复调用，调用方依赖的行为是否仍然成立？这一步能防止只验证 happy path。

本轮的验收标准是：大量相似对象真正重复的是什么，哪些状态不能共享？ 完成后，大量细粒度对象的重复内存下降。如果测试仍然必须启动所有外部服务，或仍要修改同一个大分支，说明这一步只换了名字，没有形成可验证的边界。

### 与前后两轮的连接

下一轮 P46 会继续检验这条边界：如果新增需求仍要修改本轮稳定代码，说明边界还没有放在真正的变化轴上。

不要把本轮结构当成永久答案。只要真实需求证明它没有减少修改范围，就应该回退一层，删除不必要的间接调用，再重新找变化轴。

## 第 2 轮需求：先解决问题，最后再叫它 享元工厂（P46）

我们继续修改同一个“需要渲染百万格地图瓦片的游戏场景”，不另起一个玩具项目。本轮的新压力是：**把坐标留在瓦片，把不可变类型放入按键缓存，并用对象身份测量共享结果**。先不要背模式名，先列出要改的判断、对象、外部依赖和测试；这份清单就是“修改半径”。

这一轮真正要回答的是：怎样证明对象共享真的节省内存，而不是凭感觉增加复杂度？ 上一轮 P45 已经处理“Flyweight”；这一轮不要把它推倒重来，而是只处理新的变化轴。

### 先记录修改半径

- 当前泄漏：地图每个格子都创建完整纹理与碰撞配置。
- 设计动作：享元工厂按稳定键缓存不可变 TileType，坐标留给 Tile。
- 稳定结果：纹理和规则只保存一次，实例只保留外部状态。
- 仍需承担的代价：先用内存剖析验证收益，低基数对象不值得引入缓存。

如果四项里只有“新增一个类”而没有说明哪种变化被隔离，先不要合并这次重构。设计的证据不是角色数量，而是下一次同类需求能否只新增实现、只替换适配器或只调整组合根。

### 这一轮只做一个设计动作

本轮只落实 **享元（二）：用共享瓦片降低地图对象开销**，围绕“享元工厂、缓存键、不可变共享、性能测量”移动一个边界，不同时引入后续模式。把稳定流程写成调用方真正需要的业务语言；把供应商字段、创建细节或横切动作留在边界另一侧。

完成动作后再给它命名：这一轮对应 **享元工厂、缓存键、不可变共享、性能测量**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

### Python 版本：只看本轮新增的协作关系

```python
from dataclasses import dataclass

@dataclass(frozen=True)
class TileType:
    name: str
    texture: str

class TileTypePool:
    def __init__(self) -> None:
        self._cache: dict[str, TileType] = {}

    def get(self, name: str) -> TileType:
        if name not in self._cache:
            self._cache[name] = TileType(name, f"{name}.png")
        return self._cache[name]

@dataclass(frozen=True)
class Tile:
    x: int
    y: int
    kind: TileType

pool = TileTypePool()
tile = Tile(12, 8, pool.get("forest"))
print(tile, pool.get("forest") is tile.kind)
```

读这段代码时只检查三件事：谁拥有规则、谁负责组合、谁可以被替换。不要因为示例用了 Protocol、注册表或装饰器，就默认生产代码也必须采用同样的层数；真正需要保留的是边界和行为契约。

### 用一个失败场景验证边界

先写一个会失败的测试，再写实现：新增一种“游戏地图瓦片”变化时，哪些稳定代码应该保持不动？如果实现抛出异常、返回空值或收到重复调用，调用方依赖的行为是否仍然成立？这一步能防止只验证 happy path。

本轮的验收标准是：怎样证明对象共享真的节省内存，而不是凭感觉增加复杂度？ 完成后，纹理和规则只保存一次，实例只保留外部状态。如果测试仍然必须启动所有外部服务，或仍要修改同一个大分支，说明这一步只换了名字，没有形成可验证的边界。

### 与前后两轮的连接

这是本章最后一轮，接下来要把这条边界放回完整实现和 UML 中，检查它是否真的减少了修改半径。

不要把本轮结构当成永久答案。只要真实需求证明它没有减少修改范围，就应该回退一层，删除不必要的间接调用，再重新找变化轴。


## 演进完成后的 Python 实现

下面不是另一个示例，而是起点代码承受完上述需求后的版本。每个新增角色都能追溯到上面的某一轮真实修改压力；删掉任何一层，都应该能说出哪项需求会重新变难。阅读顺序建议是：先看依赖方向，再看对象拥有谁，最后看异常和替换测试。

```python
from dataclasses import dataclass

@dataclass(frozen=True)
class TileType:
    texture: str
    walkable: bool
@dataclass(frozen=True)
class Tile:
    x: int
    y: int
    kind: TileType
class TileTypes:
    def __init__(self) -> None: self._cache: dict[tuple[str, bool], TileType] = {}
    def get(self, texture: str, walkable: bool) -> TileType:
        key = (texture, walkable)
        return self._cache.setdefault(key, TileType(*key))
```

![享元、状态分离与内存优化：先测量，再共享：案例 UML 结构与对象关系](/images/notes/programming-thoughts/diagrams/chapter-flyweight-state-memory-uml.svg)

这张 UML 只画最终需要长期沟通的关系。虚线强调依赖或接口实现，菱形表示对象拥有或包装另一个对象；创建和调用细节仍以 Python 代码为准。

## 用真实业务结果验证，而不是测试模式名

测试不关心类图里有几个角色，只验证稳定业务结果、替换能力和关键失败边界。下面代码与上一个代码块拼接后可直接由 Python 3.12 执行。

```python
types = TileTypes()
tiles = [Tile(x, 0, types.get("grass.png", True)) for x in range(1000)]
assert len({id(tile.kind) for tile in tiles}) == 1
assert tiles[0].x != tiles[-1].x
```

![享元、状态分离与内存优化：先测量，再共享：同一份代码重构前后结构对照](/images/notes/programming-thoughts/diagrams/chapter-flyweight-state-memory-comparison.svg)

## 把连续重构放回一张决策表

| 检查项 | 仍然简单时 | 变化已经重复时 | 抽象后的验证 |
| --- | --- | --- | --- |
| 修改范围 | 一个分支或一个函数 | 同类判断散落到多个模块 | 新需求主要新增实现 |
| 依赖语言 | 具体类名与外部字段 | 业务规则被实现细节牵着走 | 调用方只看自己的业务能力 |
| 测试方式 | 端到端手工验证 | 每次都要启动整套环境 | 稳定规则可用替身局部测试 |
| 撤销成本 | 几乎没有 | 继续堆分支的成本持续上升 | 抽象仍能被一次小重构移除 |

这四行里最容易被忽略的是“撤销成本”。好的演进式设计不是层数最多，而是每一步都能解释、能验证、也能退回去。没有第二个真实用例时，保留直接实现通常更诚实；出现重复变化后仍拒绝命名边界，则会把复杂度转移给未来维护者。

## 一次完整复盘：从需求到代码审查

提交一次重构时，可以按下面顺序写审查说明：

1. 变化由谁发起？是业务规则、供应商协议、对象构造、运行时策略，还是横切关注点？
2. 这次修改触碰了哪些文件、分支和测试？重构后修改半径缩短了吗？
3. 新边界暴露的是业务语言，还是把旧实现的字段名换了一个包装？
4. 替换实现时，前置条件、返回值、异常和副作用是否仍然符合契约？
5. 如果未来发现抽象没有收益，能否只撤销本轮，而不牵动整个系统？

把这五问写进 Code Review，比在类名上争论“这是不是标准模式”更有用。它们也能帮助你把视频里的抽象迁移到完全不同的业务场景。

## 为什么这些分 P 必须连在一起读

单独看每个名词，很容易把设计理解成一组互不相关的技巧。放回同一条演进链后会发现，前一步通常在制造后一步所需的条件：先把变化聚到局部，才看得见稳定边界；先让依赖显式，才谈得上替换；先定义替换契约，才有资格把实现交给工厂、注册表或组合根。

同样，后一种结构并不会淘汰前一种。一个系统完全可能在入口保留简单分支，在核心流程使用协议，在基础设施侧使用注册表。选择标准不是“哪个模式更高级”，而是当前变化由谁发起、发生频率多高、失败后需要谁负责。

可以用三次追问检查理解是否连贯。第一，去掉新抽象后，哪一个真实需求会重新变难？第二，新增一种实现时，哪些稳定代码仍然会被修改？第三，实现抛错、超时或返回边界值时，调用方依赖的行为是否仍然成立？如果答不出来，先回到上一个更简单的版本。

本文把代码拆成多个小切片，是为了让每次决策清楚，并不建议在项目中为每个概念各建一层。真正落地时，应该把相邻且同向变化的角色合并，把仅服务一个调用点的抽象留在模块内部，只把跨模块需要共同遵守的契约公开出去。这样既保留演进路径，也不会把教学结构原样搬成生产结构。

## 容易走偏的地方

1. **第一版就设计最终形态。** 这会跳过最重要的证据收集，也让团队无法解释每层抽象来自哪个需求。
2. **用类的数量衡量设计。** 类少不等于简单，类多也不等于解耦；真正要看的是一次变化传播多远。
3. **只画重构后的图。** 没有“之前为什么痛”，读者只能背结构，无法迁移到新的问题。
4. **把 Protocol 当作自动解耦。** 如果接口仍然复制供应商语言，业务只是隔着一层继续依赖供应商。
5. **忽略运行时与失败语义。** 能正常返回只是契约的一部分，超时、重复、并发和部分失败同样决定实现能否替换。

## 什么时候停在更简单的版本

如果实现只有一个、变化方向长期稳定、团队无法给出第二个真实需求，先停在函数或小类即可。本文的每次重构都有前提：旧结构已经让某种变化重复付费。没有这个前提，所谓扩展性很容易变成需要长期维护的猜测。

反过来，如果同一个条件分支已经复制三次、测试必须连接真实基础设施、或一个调用方需要穿过三层对象才能回答业务问题，就不要再用“以后再说”掩盖结构债务。此时先写修改清单，再选择本文最小的一步，而不是一次引入所有模式。

## 练习：把视频推导重新走一遍

为“需要渲染百万格地图瓦片的游戏场景”增加一个与现有实现明显不同的需求。第一轮禁止增加接口，只修改朴素版本并记录触碰点；第二轮使用本文对应步骤重构；第三轮再加入一个变化，验证旧流程是否保持不动。

提交物包括：重构前后的修改清单、一张依赖方向图、至少两个业务测试、一个共享契约测试，以及一段不超过 150 字的代价说明。最后回看 P45–P46，确认每个结构都能追溯到一个真实失败，而不是追溯到一个想背下来的名词。

## 小结

本章真正形成的是一条推理链：直接实现 → 新需求暴露耦合 → 找到变化轴 → 放置最小边界 → 用测试证明 → 写下代价。做到这一步，Flyweight、享元工厂 才不再是彼此分散的知识点，而是同一段代码演进过程中自然出现的工具。
