---
title: P16–P21：从模式语言到单例生命周期：唯一实例不等于全局变量
description: 以“需要延迟加载、测试替换和进程级复用的配置目录”为贯穿案例，按视频顺序从朴素实现、需求失败到重构命名，渐进理解 模式语言、饿汉式、模块对象、构造守卫、缓存函数、全局状态。
publishDate: 2026-08-23
updatedDate: 2026-08-27
language: zh
---

这不是把 P16–P21 的定义拼成一篇词典，而是让一个系统连续经历几轮需求。我们从 **需要延迟加载、测试替换和进程级复用的配置目录** 的最直接实现出发；每当代码真的承受不住下一次变化，才引入一个新边界。

最终要回答的是：**Python 里怎样表达“一个实例”，又不把依赖藏成全局状态？** 学完的验收标准也不是记住名词，而是能区分实例数量、创建时机、访问方式和作用域。

## 先看完整推导路线

| 阶段 | 原视频 | 要解决的问题 | 具体设计动作 | 保护的结果 |
| --- | --- | --- | --- | --- |
| 1 | P16 | 为什么不理解约束，照着类图也会写出错误的设计模式？ | 看到模式名就照搬类图，却说不清问题 | 用“上下文—冲突力量—结构—后果”描述设计 | 团队讨论从类名转向真正的工程权衡 |
| 2 | P17 | 延迟创建换来了什么，又必须补上哪些并发保证？ | 多个线程可能同时创建懒加载实例 | 比较启动时创建、同步懒加载与双重检查的保证 | 明确初始化时机与并发语义 |
| 3 | P18 | 为什么 Python 更适合用模块边界，而不是模拟私有构造器？ | 模块级状态被不同入口重复创建或随意修改 | 优先使用模块对象和显式工厂守住唯一实例边界 | 调用方不需要模拟私有构造器，测试也能替换依赖 |
| 4 | P19 | 哪些防护只是补丁，哪些保证由 Python 运行时提供？ | 模块级状态被不同入口重复创建或随意修改 | 用 Python 模块、Enum 或显式注册表承载唯一实例不变量 | 实例边界清楚，调用方不需要绕过构造器 |
| 5 | P20 | 如何利用 Python 的缓存函数避免手写双重检查锁？ | 为了延迟初始化手写复杂锁代码 | 利用 Python 的缓存函数按需创建并复用实例 | 代码短，初始化时机和缓存范围都可见 |
| 6 | P21 | 什么时候需要一个实例，什么时候只是为了少传一个参数？ | 把“只有一个实例”误写成任意位置都能访问的全局变量 | 区分实例数量、生命周期和获取方式 | 可由容器管理单实例，同时仍显式注入 |

这张表不是术语目录，而是一份修改记录。每一行都要能回答：变化从哪里来、边界放在哪里、什么结果不应该被碰。阅读正文时建议停在每个“新需求”之前，先自己写修改清单，再比较后面的重构。

## 本章总结：把每个 P 压缩成一张决策卡

### P16 · 设计模式导入：从名字回到问题、约束与代价

**问题**：看到模式名就照搬类图，却说不清问题。

**动作**：用“上下文—冲突力量—结构—后果”描述设计。

**验证**：团队讨论从类名转向真正的工程权衡。

**代价**：模式不是目标，简单函数能解决时无需引入角色。

### P17 · 单例（一）：饿汉式、懒汉式与线程安全

**问题**：多个线程可能同时创建懒加载实例。

**动作**：比较启动时创建、同步懒加载与双重检查的保证。

**验证**：明确初始化时机与并发语义。

**代价**：全局实例会隐藏依赖并污染测试，不要默认使用。

### P18 · 单例（二）：Python 模块如何守住唯一实例

**问题**：模块级状态被不同入口重复创建或随意修改。

**动作**：优先使用模块对象和显式工厂守住唯一实例边界。

**验证**：调用方不需要模拟私有构造器，测试也能替换依赖。

**代价**：普通业务不需要为了假想的反射攻击制造复杂框架。

### P19 · 单例（三）：反射防护与枚举方案

**问题**：模块级状态被不同入口重复创建或随意修改。

**动作**：用 Python 模块、Enum 或显式注册表承载唯一实例不变量。

**验证**：实例边界清楚，调用方不需要绕过构造器。

**代价**：进程单例仍可能隐藏依赖，不要把请求态数据放进去。

### P20 · 单例（四）：缓存函数的延迟初始化

**问题**：为了延迟初始化手写复杂锁代码。

**动作**：利用 Python 的缓存函数按需创建并复用实例。

**验证**：代码短，初始化时机和缓存范围都可见。

**代价**：这不是并发共享状态的万能方案，先确认生命周期。

### P21 · 单例总结：唯一实例不等于全局可访问

**问题**：把“只有一个实例”误写成任意位置都能访问的全局变量。

**动作**：区分实例数量、生命周期和获取方式。

**验证**：可由容器管理单实例，同时仍显式注入。

**代价**：请求态、租户态和测试态数据不应放进进程单例。

这些卡片故意把“代价”放在“验证”后面。先证明边界解决了真实问题，再讨论多了一层接口、注册表、包装器或组合根是否值得。没有代价说明的设计总结，通常只是模式宣传。

![从模式语言到单例生命周期：唯一实例不等于全局变量：同一案例随需求逐步演进](/images/notes/programming-thoughts/diagrams/chapter-patterns-and-singleton-lifecycle.svg)

## 起点：先写最直接、能工作的版本

这一版故意把决策和实现放在一起。它不是“错误代码”，而是需求尚少时合理的最低成本方案。请先运行它，并标出每个业务决定；接下来的 P16–P21 会连续修改同一案例。

```python
catalog = None

def get_catalog():
    global catalog
    if catalog is None: catalog = load_from_disk()
    return catalog
```

## 第 1 轮需求：先解决问题，最后再叫它 模式语言（P16）

我们继续修改同一个“需要延迟加载、测试替换和进程级复用的配置目录”，不另起一个玩具项目。本轮的新压力是：**先用上下文、冲突、结构和后果描述配置目录问题，不急着选择模式**。先不要背模式名，先列出要改的判断、对象、外部依赖和测试；这份清单就是“修改半径”。

这一轮真正要回答的是：为什么不理解约束，照着类图也会写出错误的设计模式？ 这是本章的第一轮，先保留最直接的实现，记录它面对下一次需求时会触碰哪些地方。

### 先记录修改半径

- 当前泄漏：看到模式名就照搬类图，却说不清问题。
- 设计动作：用“上下文—冲突力量—结构—后果”描述设计。
- 稳定结果：团队讨论从类名转向真正的工程权衡。
- 仍需承担的代价：模式不是目标，简单函数能解决时无需引入角色。

如果四项里只有“新增一个类”而没有说明哪种变化被隔离，先不要合并这次重构。设计的证据不是角色数量，而是下一次同类需求能否只新增实现、只替换适配器或只调整组合根。

### 这一轮只做一个设计动作

本轮只落实 **设计模式导入：从名字回到问题、约束与代价**，围绕“模式语言、适用场景、权衡、反模式”移动一个边界，不同时引入后续模式。把稳定流程写成调用方真正需要的业务语言；把供应商字段、创建细节或横切动作留在边界另一侧。

完成动作后再给它命名：这一轮对应 **模式语言、适用场景、权衡、反模式**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

### Python 版本：只看本轮新增的协作关系

```python
from dataclasses import dataclass

@dataclass(frozen=True)
class DesignDecision:
    context: str
    conflict: str
    structure: str
    consequence: str

decision = DesignDecision(
    "通知渠道会增加",
    "稳定流程不应了解每个 SDK",
    "Notifier 协议 + 多个实现",
    "新增渠道只增加实现",
)
print(decision.structure)
```

读这段代码时只检查三件事：谁拥有规则、谁负责组合、谁可以被替换。不要因为示例用了 Protocol、注册表或装饰器，就默认生产代码也必须采用同样的层数；真正需要保留的是边界和行为契约。

### 用一个失败场景验证边界

先写一个会失败的测试，再写实现：新增一种“支付渠道演进”变化时，哪些稳定代码应该保持不动？如果实现抛出异常、返回空值或收到重复调用，调用方依赖的行为是否仍然成立？这一步能防止只验证 happy path。

本轮的验收标准是：为什么不理解约束，照着类图也会写出错误的设计模式？ 完成后，团队讨论从类名转向真正的工程权衡。如果测试仍然必须启动所有外部服务，或仍要修改同一个大分支，说明这一步只换了名字，没有形成可验证的边界。

### 与前后两轮的连接

下一轮 P17 会继续检验这条边界：如果新增需求仍要修改本轮稳定代码，说明边界还没有放在真正的变化轴上。

不要把本轮结构当成永久答案。只要真实需求证明它没有减少修改范围，就应该回退一层，删除不必要的间接调用，再重新找变化轴。

## 第 2 轮需求：先解决问题，最后再叫它 饿汉式（P17）

我们继续修改同一个“需要延迟加载、测试替换和进程级复用的配置目录”，不另起一个玩具项目。本轮的新压力是：**配置加载昂贵且只需一次，开始比较立即创建与延迟创建**。先不要背模式名，先列出要改的判断、对象、外部依赖和测试；这份清单就是“修改半径”。

这一轮真正要回答的是：延迟创建换来了什么，又必须补上哪些并发保证？ 上一轮 P16 已经处理“模式语言”；这一轮不要把它推倒重来，而是只处理新的变化轴。

### 先记录修改半径

- 当前泄漏：多个线程可能同时创建懒加载实例。
- 设计动作：比较启动时创建、同步懒加载与双重检查的保证。
- 稳定结果：明确初始化时机与并发语义。
- 仍需承担的代价：全局实例会隐藏依赖并污染测试，不要默认使用。

如果四项里只有“新增一个类”而没有说明哪种变化被隔离，先不要合并这次重构。设计的证据不是角色数量，而是下一次同类需求能否只新增实现、只替换适配器或只调整组合根。

### 这一轮只做一个设计动作

本轮只落实 **单例（一）：饿汉式、懒汉式与线程安全**，围绕“饿汉式、懒汉式、线程安全、生命周期”移动一个边界，不同时引入后续模式。把稳定流程写成调用方真正需要的业务语言；把供应商字段、创建细节或横切动作留在边界另一侧。

完成动作后再给它命名：这一轮对应 **饿汉式、懒汉式、线程安全、生命周期**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

### Python 版本：只看本轮新增的协作关系

```python
from functools import lru_cache

class Config:
    def __init__(self, env: str) -> None:
        self.env = env

@lru_cache(maxsize=1)
def config() -> Config:
    return Config("production")

first = config()
second = config()
assert first is second
print(first.env)
```

读这段代码时只检查三件事：谁拥有规则、谁负责组合、谁可以被替换。不要因为示例用了 Protocol、注册表或装饰器，就默认生产代码也必须采用同样的层数；真正需要保留的是边界和行为契约。

### 用一个失败场景验证边界

先写一个会失败的测试，再写实现：新增一种“只读配置目录”变化时，哪些稳定代码应该保持不动？如果实现抛出异常、返回空值或收到重复调用，调用方依赖的行为是否仍然成立？这一步能防止只验证 happy path。

本轮的验收标准是：延迟创建换来了什么，又必须补上哪些并发保证？ 完成后，明确初始化时机与并发语义。如果测试仍然必须启动所有外部服务，或仍要修改同一个大分支，说明这一步只换了名字，没有形成可验证的边界。

### 与前后两轮的连接

下一轮 P18 会继续检验这条边界：如果新增需求仍要修改本轮稳定代码，说明边界还没有放在真正的变化轴上。

不要把本轮结构当成永久答案。只要真实需求证明它没有减少修改范围，就应该回退一层，删除不必要的间接调用，再重新找变化轴。

## 第 3 轮需求：先解决问题，最后再叫它 模块对象（P18）

我们继续修改同一个“需要延迟加载、测试替换和进程级复用的配置目录”，不另起一个玩具项目。本轮的新压力是：**模块被多处导入，唯一实例边界应由 Python 模块而非私有构造器表达**。先不要背模式名，先列出要改的判断、对象、外部依赖和测试；这份清单就是“修改半径”。

这一轮真正要回答的是：为什么 Python 更适合用模块边界，而不是模拟私有构造器？ 上一轮 P17 已经处理“饿汉式”；这一轮不要把它推倒重来，而是只处理新的变化轴。

### 先记录修改半径

- 当前泄漏：模块级状态被不同入口重复创建或随意修改。
- 设计动作：优先使用模块对象和显式工厂守住唯一实例边界。
- 稳定结果：调用方不需要模拟私有构造器，测试也能替换依赖。
- 仍需承担的代价：普通业务不需要为了假想的反射攻击制造复杂框架。

如果四项里只有“新增一个类”而没有说明哪种变化被隔离，先不要合并这次重构。设计的证据不是角色数量，而是下一次同类需求能否只新增实现、只替换适配器或只调整组合根。

### 这一轮只做一个设计动作

本轮只落实 **单例（二）：Python 模块如何守住唯一实例**，围绕“模块对象、构造边界、不变量、攻击面”移动一个边界，不同时引入后续模式。把稳定流程写成调用方真正需要的业务语言；把供应商字段、创建细节或横切动作留在边界另一侧。

完成动作后再给它命名：这一轮对应 **模块对象、构造边界、不变量、攻击面**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

### Python 版本：只看本轮新增的协作关系

```python
class Config:
    def __init__(self, env: str) -> None:
        self.env = env

_shared = Config("production")

def config() -> Config:
    return _shared

# Python 模块本身就是自然的单例边界；不需要用反射绕过私有构造器。
assert config() is config()
```

读这段代码时只检查三件事：谁拥有规则、谁负责组合、谁可以被替换。不要因为示例用了 Protocol、注册表或装饰器，就默认生产代码也必须采用同样的层数；真正需要保留的是边界和行为契约。

### 用一个失败场景验证边界

先写一个会失败的测试，再写实现：新增一种“Python 配置中心”变化时，哪些稳定代码应该保持不动？如果实现抛出异常、返回空值或收到重复调用，调用方依赖的行为是否仍然成立？这一步能防止只验证 happy path。

本轮的验收标准是：为什么 Python 更适合用模块边界，而不是模拟私有构造器？ 完成后，调用方不需要模拟私有构造器，测试也能替换依赖。如果测试仍然必须启动所有外部服务，或仍要修改同一个大分支，说明这一步只换了名字，没有形成可验证的边界。

### 与前后两轮的连接

下一轮 P19 会继续检验这条边界：如果新增需求仍要修改本轮稳定代码，说明边界还没有放在真正的变化轴上。

不要把本轮结构当成永久答案。只要真实需求证明它没有减少修改范围，就应该回退一层，删除不必要的间接调用，再重新找变化轴。

## 第 4 轮需求：先解决问题，最后再叫它 构造守卫（P19）

我们继续修改同一个“需要延迟加载、测试替换和进程级复用的配置目录”，不另起一个玩具项目。本轮的新压力是：**注册表也要求唯一身份，但可变状态必须留在明确边界内**。先不要背模式名，先列出要改的判断、对象、外部依赖和测试；这份清单就是“修改半径”。

这一轮真正要回答的是：哪些防护只是补丁，哪些保证由 Python 运行时提供？ 上一轮 P18 已经处理“模块对象”；这一轮不要把它推倒重来，而是只处理新的变化轴。

### 先记录修改半径

- 当前泄漏：模块级状态被不同入口重复创建或随意修改。
- 设计动作：用 Python 模块、Enum 或显式注册表承载唯一实例不变量。
- 稳定结果：实例边界清楚，调用方不需要绕过构造器。
- 仍需承担的代价：进程单例仍可能隐藏依赖，不要把请求态数据放进去。

如果四项里只有“新增一个类”而没有说明哪种变化被隔离，先不要合并这次重构。设计的证据不是角色数量，而是下一次同类需求能否只新增实现、只替换适配器或只调整组合根。

### 这一轮只做一个设计动作

本轮只落实 **单例（三）：反射防护与枚举方案**，围绕“构造守卫、Enum 单例、序列化、边界”移动一个边界，不同时引入后续模式。把稳定流程写成调用方真正需要的业务语言；把供应商字段、创建细节或横切动作留在边界另一侧。

完成动作后再给它命名：这一轮对应 **构造守卫、Enum 单例、序列化、边界**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

### Python 版本：只看本轮新增的协作关系

```python
from enum import Enum

class Registry(Enum):
    INSTANCE = "instance"

    def __init__(self, _: str) -> None:
        self.handlers: dict[str, str] = {}

    def register(self, name: str, handler: str) -> None:
        self.handlers[name] = handler

Registry.INSTANCE.register("welcome", "WelcomeHandler")
print(Registry.INSTANCE.handlers)
```

读这段代码时只检查三件事：谁拥有规则、谁负责组合、谁可以被替换。不要因为示例用了 Protocol、注册表或装饰器，就默认生产代码也必须采用同样的层数；真正需要保留的是边界和行为契约。

### 用一个失败场景验证边界

先写一个会失败的测试，再写实现：新增一种“Python 模块级注册表”变化时，哪些稳定代码应该保持不动？如果实现抛出异常、返回空值或收到重复调用，调用方依赖的行为是否仍然成立？这一步能防止只验证 happy path。

本轮的验收标准是：哪些防护只是补丁，哪些保证由 Python 运行时提供？ 完成后，实例边界清楚，调用方不需要绕过构造器。如果测试仍然必须启动所有外部服务，或仍要修改同一个大分支，说明这一步只换了名字，没有形成可验证的边界。

### 与前后两轮的连接

下一轮 P20 会继续检验这条边界：如果新增需求仍要修改本轮稳定代码，说明边界还没有放在真正的变化轴上。

不要把本轮结构当成永久答案。只要真实需求证明它没有减少修改范围，就应该回退一层，删除不必要的间接调用，再重新找变化轴。

## 第 5 轮需求：先解决问题，最后再叫它 缓存函数（P20）

我们继续修改同一个“需要延迟加载、测试替换和进程级复用的配置目录”，不另起一个玩具项目。本轮的新压力是：**延迟加载需要复用成熟缓存语义，避免手写锁**。先不要背模式名，先列出要改的判断、对象、外部依赖和测试；这份清单就是“修改半径”。

这一轮真正要回答的是：如何利用 Python 的缓存函数避免手写双重检查锁？ 上一轮 P19 已经处理“构造守卫”；这一轮不要把它推倒重来，而是只处理新的变化轴。

### 先记录修改半径

- 当前泄漏：为了延迟初始化手写复杂锁代码。
- 设计动作：利用 Python 的缓存函数按需创建并复用实例。
- 稳定结果：代码短，初始化时机和缓存范围都可见。
- 仍需承担的代价：这不是并发共享状态的万能方案，先确认生命周期。

如果四项里只有“新增一个类”而没有说明哪种变化被隔离，先不要合并这次重构。设计的证据不是角色数量，而是下一次同类需求能否只新增实现、只替换适配器或只调整组合根。

### 这一轮只做一个设计动作

本轮只落实 **单例（四）：缓存函数的延迟初始化**，围绕“缓存函数、延迟初始化、生命周期、线程安全”移动一个边界，不同时引入后续模式。把稳定流程写成调用方真正需要的业务语言；把供应商字段、创建细节或横切动作留在边界另一侧。

完成动作后再给它命名：这一轮对应 **缓存函数、延迟初始化、生命周期、线程安全**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

### Python 版本：只看本轮新增的协作关系

```python
from functools import lru_cache

class MetadataCatalog:
    def __init__(self) -> None:
        self.values = {"version": "1.0"}

@lru_cache(maxsize=1)
def catalog() -> MetadataCatalog:
    return MetadataCatalog()

print(catalog().values["version"])
# 第一次调用才创建，缓存装饰器也让线程间的语义更清晰。
```

读这段代码时只检查三件事：谁拥有规则、谁负责组合、谁可以被替换。不要因为示例用了 Protocol、注册表或装饰器，就默认生产代码也必须采用同样的层数；真正需要保留的是边界和行为契约。

### 用一个失败场景验证边界

先写一个会失败的测试，再写实现：新增一种“Python 延迟元数据目录”变化时，哪些稳定代码应该保持不动？如果实现抛出异常、返回空值或收到重复调用，调用方依赖的行为是否仍然成立？这一步能防止只验证 happy path。

本轮的验收标准是：如何利用 Python 的缓存函数避免手写双重检查锁？ 完成后，代码短，初始化时机和缓存范围都可见。如果测试仍然必须启动所有外部服务，或仍要修改同一个大分支，说明这一步只换了名字，没有形成可验证的边界。

### 与前后两轮的连接

下一轮 P21 会继续检验这条边界：如果新增需求仍要修改本轮稳定代码，说明边界还没有放在真正的变化轴上。

不要把本轮结构当成永久答案。只要真实需求证明它没有减少修改范围，就应该回退一层，删除不必要的间接调用，再重新找变化轴。

## 第 6 轮需求：先解决问题，最后再叫它 全局状态（P21）

我们继续修改同一个“需要延迟加载、测试替换和进程级复用的配置目录”，不另起一个玩具项目。本轮的新压力是：**业务对象仍显式接收 Catalog，单例只属于组合根的生命周期决策**。先不要背模式名，先列出要改的判断、对象、外部依赖和测试；这份清单就是“修改半径”。

这一轮真正要回答的是：什么时候需要一个实例，什么时候只是为了少传一个参数？ 上一轮 P20 已经处理“缓存函数”；这一轮不要把它推倒重来，而是只处理新的变化轴。

### 先记录修改半径

- 当前泄漏：把“只有一个实例”误写成任意位置都能访问的全局变量。
- 设计动作：区分实例数量、生命周期和获取方式。
- 稳定结果：可由容器管理单实例，同时仍显式注入。
- 仍需承担的代价：请求态、租户态和测试态数据不应放进进程单例。

如果四项里只有“新增一个类”而没有说明哪种变化被隔离，先不要合并这次重构。设计的证据不是角色数量，而是下一次同类需求能否只新增实现、只替换适配器或只调整组合根。

### 这一轮只做一个设计动作

本轮只落实 **单例总结：唯一实例不等于全局可访问**，围绕“全局状态、作用域、可测试性、替代方案”移动一个边界，不同时引入后续模式。把稳定流程写成调用方真正需要的业务语言；把供应商字段、创建细节或横切动作留在边界另一侧。

完成动作后再给它命名：这一轮对应 **全局状态、作用域、可测试性、替代方案**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

### Python 版本：只看本轮新增的协作关系

```python
class Catalog:
    def find(self, sku: str) -> int:
        return {"book": 100}.get(sku, 0)

class CheckoutService:
    def __init__(self, catalog: Catalog) -> None:
        self.catalog = catalog

    def total(self, sku: str, count: int) -> int:
        return self.catalog.find(sku) * count

# 组合根决定共享范围；业务对象仍然显式接收依赖。
service = CheckoutService(Catalog())
assert service.total("book", 2) == 200
```

读这段代码时只检查三件事：谁拥有规则、谁负责组合、谁可以被替换。不要因为示例用了 Protocol、注册表或装饰器，就默认生产代码也必须采用同样的层数；真正需要保留的是边界和行为契约。

### 用一个失败场景验证边界

先写一个会失败的测试，再写实现：新增一种“请求上下文与应用服务”变化时，哪些稳定代码应该保持不动？如果实现抛出异常、返回空值或收到重复调用，调用方依赖的行为是否仍然成立？这一步能防止只验证 happy path。

本轮的验收标准是：什么时候需要一个实例，什么时候只是为了少传一个参数？ 完成后，可由容器管理单实例，同时仍显式注入。如果测试仍然必须启动所有外部服务，或仍要修改同一个大分支，说明这一步只换了名字，没有形成可验证的边界。

### 与前后两轮的连接

这是本章最后一轮，接下来要把这条边界放回完整实现和 UML 中，检查它是否真的减少了修改半径。

不要把本轮结构当成永久答案。只要真实需求证明它没有减少修改范围，就应该回退一层，删除不必要的间接调用，再重新找变化轴。


## 演进完成后的 Python 实现

下面不是另一个示例，而是起点代码承受完上述需求后的版本。每个新增角色都能追溯到上面的某一轮真实修改压力；删掉任何一层，都应该能说出哪项需求会重新变难。阅读顺序建议是：先看依赖方向，再看对象拥有谁，最后看异常和替换测试。

```python
from functools import lru_cache

class Catalog:
    def __init__(self, values: dict[str, str]) -> None: self.values = values
    def find(self, key: str) -> str | None: return self.values.get(key)

@lru_cache(maxsize=1)
def application_catalog() -> Catalog:
    return Catalog({"version": "1.0"})

class ShowVersion:
    def __init__(self, catalog: Catalog) -> None: self.catalog = catalog
    def run(self) -> str: return self.catalog.find("version") or "unknown"
```

![从模式语言到单例生命周期：唯一实例不等于全局变量：案例 UML 结构与对象关系](/images/notes/programming-thoughts/diagrams/chapter-patterns-and-singleton-lifecycle-uml.svg)

这张 UML 只画最终需要长期沟通的关系。虚线强调依赖或接口实现，菱形表示对象拥有或包装另一个对象；创建和调用细节仍以 Python 代码为准。

## 用真实业务结果验证，而不是测试模式名

测试不关心类图里有几个角色，只验证稳定业务结果、替换能力和关键失败边界。下面代码与上一个代码块拼接后可直接由 Python 3.12 执行。

```python
assert application_catalog() is application_catalog()
assert ShowVersion(application_catalog()).run() == "1.0"
fake = Catalog({"version": "test"})
assert ShowVersion(fake).run() == "test"
```

![从模式语言到单例生命周期：唯一实例不等于全局变量：同一份代码重构前后结构对照](/images/notes/programming-thoughts/diagrams/chapter-patterns-and-singleton-lifecycle-comparison.svg)

## 把连续重构放回一张决策表

| 检查项 | 仍然简单时 | 变化已经重复时 | 抽象后的验证 |
| --- | --- | --- | --- |
| 修改范围 | 一个分支或一个函数 | 同类判断散落到多个模块 | 新需求主要新增实现 |
| 依赖语言 | 具体类名与外部字段 | 业务规则被实现细节牵着走 | 调用方只看自己的业务能力 |
| 测试方式 | 端到端手工验证 | 每次都要启动整套环境 | 稳定规则可用替身局部测试 |
| 撤销成本 | 几乎没有 | 继续堆分支的成本持续上升 | 抽象仍能被一次小重构移除 |

这四行里最容易被忽略的是“撤销成本”。好的演进式设计不是层数最多，而是每一步都能解释、能验证、也能退回去。没有第二个真实用例时，保留直接实现通常更诚实；出现重复变化后仍拒绝命名边界，则会把复杂度转移给未来维护者。

## 一次完整复盘：从需求到代码审查

提交一次重构时，可以按下面顺序写审查说明：

1. 变化由谁发起？是业务规则、供应商协议、对象构造、运行时策略，还是横切关注点？
2. 这次修改触碰了哪些文件、分支和测试？重构后修改半径缩短了吗？
3. 新边界暴露的是业务语言，还是把旧实现的字段名换了一个包装？
4. 替换实现时，前置条件、返回值、异常和副作用是否仍然符合契约？
5. 如果未来发现抽象没有收益，能否只撤销本轮，而不牵动整个系统？

把这五问写进 Code Review，比在类名上争论“这是不是标准模式”更有用。它们也能帮助你把视频里的抽象迁移到完全不同的业务场景。

## 为什么这些分 P 必须连在一起读

单独看每个名词，很容易把设计理解成一组互不相关的技巧。放回同一条演进链后会发现，前一步通常在制造后一步所需的条件：先把变化聚到局部，才看得见稳定边界；先让依赖显式，才谈得上替换；先定义替换契约，才有资格把实现交给工厂、注册表或组合根。

同样，后一种结构并不会淘汰前一种。一个系统完全可能在入口保留简单分支，在核心流程使用协议，在基础设施侧使用注册表。选择标准不是“哪个模式更高级”，而是当前变化由谁发起、发生频率多高、失败后需要谁负责。

可以用三次追问检查理解是否连贯。第一，去掉新抽象后，哪一个真实需求会重新变难？第二，新增一种实现时，哪些稳定代码仍然会被修改？第三，实现抛错、超时或返回边界值时，调用方依赖的行为是否仍然成立？如果答不出来，先回到上一个更简单的版本。

本文把代码拆成多个小切片，是为了让每次决策清楚，并不建议在项目中为每个概念各建一层。真正落地时，应该把相邻且同向变化的角色合并，把仅服务一个调用点的抽象留在模块内部，只把跨模块需要共同遵守的契约公开出去。这样既保留演进路径，也不会把教学结构原样搬成生产结构。

## 容易走偏的地方

1. **第一版就设计最终形态。** 这会跳过最重要的证据收集，也让团队无法解释每层抽象来自哪个需求。
2. **用类的数量衡量设计。** 类少不等于简单，类多也不等于解耦；真正要看的是一次变化传播多远。
3. **只画重构后的图。** 没有“之前为什么痛”，读者只能背结构，无法迁移到新的问题。
4. **把 Protocol 当作自动解耦。** 如果接口仍然复制供应商语言，业务只是隔着一层继续依赖供应商。
5. **忽略运行时与失败语义。** 能正常返回只是契约的一部分，超时、重复、并发和部分失败同样决定实现能否替换。

## 什么时候停在更简单的版本

如果实现只有一个、变化方向长期稳定、团队无法给出第二个真实需求，先停在函数或小类即可。本文的每次重构都有前提：旧结构已经让某种变化重复付费。没有这个前提，所谓扩展性很容易变成需要长期维护的猜测。

反过来，如果同一个条件分支已经复制三次、测试必须连接真实基础设施、或一个调用方需要穿过三层对象才能回答业务问题，就不要再用“以后再说”掩盖结构债务。此时先写修改清单，再选择本文最小的一步，而不是一次引入所有模式。

## 练习：把视频推导重新走一遍

为“需要延迟加载、测试替换和进程级复用的配置目录”增加一个与现有实现明显不同的需求。第一轮禁止增加接口，只修改朴素版本并记录触碰点；第二轮使用本文对应步骤重构；第三轮再加入一个变化，验证旧流程是否保持不动。

提交物包括：重构前后的修改清单、一张依赖方向图、至少两个业务测试、一个共享契约测试，以及一段不超过 150 字的代价说明。最后回看 P16–P21，确认每个结构都能追溯到一个真实失败，而不是追溯到一个想背下来的名词。

## 小结

本章真正形成的是一条推理链：直接实现 → 新需求暴露耦合 → 找到变化轴 → 放置最小边界 → 用测试证明 → 写下代价。做到这一步，模式语言、饿汉式、模块对象、构造守卫、缓存函数、全局状态 才不再是彼此分散的知识点，而是同一段代码演进过程中自然出现的工具。
