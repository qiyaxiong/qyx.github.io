---
title: P16–P21：从模式语言到单例生命周期：唯一实例不等于全局变量
description: 以“需要延迟加载、测试替换和进程级复用的配置目录”为贯穿案例，按视频顺序从朴素实现、需求失败到重构命名，渐进理解 模式语言、饿汉式、模块对象、构造守卫、缓存函数、全局状态。
publishDate: 2026-08-23
updatedDate: 2026-08-23
language: zh
---

这不是把 P16–P21 的定义拼成一篇词典，而是让一个系统连续经历几轮需求。我们从 **需要延迟加载、测试替换和进程级复用的配置目录** 的最直接实现出发；每当代码真的承受不住下一次变化，才引入一个新边界。

最终要回答的是：**Python 里怎样表达“一个实例”，又不把依赖藏成全局状态？** 学完的验收标准也不是记住名词，而是能区分实例数量、创建时机、访问方式和作用域。

## 先看完整推导路线

| 阶段 | 原视频 | 新出现的问题 | 到最后才命名 |
| --- | --- | --- | --- |
| 1 | P16 | 为什么不理解约束，照着类图也会写出错误的设计模式？ | 模式语言 |
| 2 | P17 | 延迟创建换来了什么，又必须补上哪些并发保证？ | 饿汉式 |
| 3 | P18 | 为什么 Python 更适合用模块边界，而不是模拟私有构造器？ | 模块对象 |
| 4 | P19 | 哪些防护只是补丁，哪些保证由 Python 运行时提供？ | 构造守卫 |
| 5 | P20 | 如何利用 Python 的缓存函数避免手写双重检查锁？ | 缓存函数 |
| 6 | P21 | 什么时候需要一个实例，什么时候只是为了少传一个参数？ | 全局状态 |

这张表只是地图，不是答案。阅读正文时建议停在每个“第二个需求”之前，先自己修改一次朴素代码，再比较后面的重构。模式真正进入肌肉记忆，靠的不是看懂类图，而是亲手感受修改范围如何扩大。

![从模式语言到单例生命周期：唯一实例不等于全局变量：同一案例随需求逐步演进](/images/notes/programming-thoughts/diagrams/chapter-patterns-and-singleton-lifecycle.svg)

## 起点：先写最直接、能工作的版本

这一版故意把决策和实现放在一起。它不是“错误代码”，而是需求尚少时合理的最低成本方案。请先运行它，并标出每个业务决定；接下来的 P16–P21 会连续修改同一案例。

```python
catalog = None

def get_catalog():
    global catalog
    if catalog is None: catalog = load_from_disk()
    return catalog
```

## 第 1 轮需求：先解决问题，最后再叫它 模式语言（P16）

我们继续修改同一个“需要延迟加载、测试替换和进程级复用的配置目录”，不另起一个玩具项目。上一轮代码能工作，但新的需求让这个薄弱处出现：**先用上下文、冲突、结构和后果描述配置目录问题，不急着选择模式**。先不要套类图，先写下如果沿用原结构，需要改哪些判断、对象和测试。

这一轮真正的问题是：为什么不理解约束，照着类图也会写出错误的设计模式？ 最直接的选择仍然是继续修改原函数；第一次这样做通常最便宜。等相同方向的修改再次出现，我们才获得足够证据，知道应该把哪部分从主流程中分离。

### 这一轮只做一个设计动作

本轮只落实 **设计模式导入：从名字回到问题、约束与代价**：围绕“模式语言、适用场景、权衡、反模式”移动一个边界，不同时引入后续模式。验证标准也很具体——完成本轮需求后，再增加同方向实现时，上一轮已经稳定的业务结果不需要改写。

完成动作后再给它命名：这一轮对应 **模式语言、适用场景、权衡、反模式**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

这一轮也增加了一层命名和间接调用。下一轮需求会继续检验它；如果修改清单没有缩短，就退回更直接的版本，而不是继续叠抽象。

## 第 2 轮需求：先解决问题，最后再叫它 饿汉式（P17）

我们继续修改同一个“需要延迟加载、测试替换和进程级复用的配置目录”，不另起一个玩具项目。上一轮代码能工作，但新的需求让这个薄弱处出现：**配置加载昂贵且只需一次，开始比较立即创建与延迟创建**。先不要套类图，先写下如果沿用原结构，需要改哪些判断、对象和测试。

这一轮真正的问题是：延迟创建换来了什么，又必须补上哪些并发保证？ 最直接的选择仍然是继续修改原函数；第一次这样做通常最便宜。等相同方向的修改再次出现，我们才获得足够证据，知道应该把哪部分从主流程中分离。

### 这一轮只做一个设计动作

本轮只落实 **单例（一）：饿汉式、懒汉式与线程安全**：围绕“饿汉式、懒汉式、线程安全、生命周期”移动一个边界，不同时引入后续模式。验证标准也很具体——完成本轮需求后，再增加同方向实现时，上一轮已经稳定的业务结果不需要改写。

完成动作后再给它命名：这一轮对应 **饿汉式、懒汉式、线程安全、生命周期**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

这一轮也增加了一层命名和间接调用。下一轮需求会继续检验它；如果修改清单没有缩短，就退回更直接的版本，而不是继续叠抽象。

## 第 3 轮需求：先解决问题，最后再叫它 模块对象（P18）

我们继续修改同一个“需要延迟加载、测试替换和进程级复用的配置目录”，不另起一个玩具项目。上一轮代码能工作，但新的需求让这个薄弱处出现：**模块被多处导入，唯一实例边界应由 Python 模块而非私有构造器表达**。先不要套类图，先写下如果沿用原结构，需要改哪些判断、对象和测试。

这一轮真正的问题是：为什么 Python 更适合用模块边界，而不是模拟私有构造器？ 最直接的选择仍然是继续修改原函数；第一次这样做通常最便宜。等相同方向的修改再次出现，我们才获得足够证据，知道应该把哪部分从主流程中分离。

### 这一轮只做一个设计动作

本轮只落实 **单例（二）：Python 模块如何守住唯一实例**：围绕“模块对象、构造边界、不变量、攻击面”移动一个边界，不同时引入后续模式。验证标准也很具体——完成本轮需求后，再增加同方向实现时，上一轮已经稳定的业务结果不需要改写。

完成动作后再给它命名：这一轮对应 **模块对象、构造边界、不变量、攻击面**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

这一轮也增加了一层命名和间接调用。下一轮需求会继续检验它；如果修改清单没有缩短，就退回更直接的版本，而不是继续叠抽象。

## 第 4 轮需求：先解决问题，最后再叫它 构造守卫（P19）

我们继续修改同一个“需要延迟加载、测试替换和进程级复用的配置目录”，不另起一个玩具项目。上一轮代码能工作，但新的需求让这个薄弱处出现：**注册表也要求唯一身份，但可变状态必须留在明确边界内**。先不要套类图，先写下如果沿用原结构，需要改哪些判断、对象和测试。

这一轮真正的问题是：哪些防护只是补丁，哪些保证由 Python 运行时提供？ 最直接的选择仍然是继续修改原函数；第一次这样做通常最便宜。等相同方向的修改再次出现，我们才获得足够证据，知道应该把哪部分从主流程中分离。

### 这一轮只做一个设计动作

本轮只落实 **单例（三）：反射防护与枚举方案**：围绕“构造守卫、Enum 单例、序列化、边界”移动一个边界，不同时引入后续模式。验证标准也很具体——完成本轮需求后，再增加同方向实现时，上一轮已经稳定的业务结果不需要改写。

完成动作后再给它命名：这一轮对应 **构造守卫、Enum 单例、序列化、边界**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

这一轮也增加了一层命名和间接调用。下一轮需求会继续检验它；如果修改清单没有缩短，就退回更直接的版本，而不是继续叠抽象。

## 第 5 轮需求：先解决问题，最后再叫它 缓存函数（P20）

我们继续修改同一个“需要延迟加载、测试替换和进程级复用的配置目录”，不另起一个玩具项目。上一轮代码能工作，但新的需求让这个薄弱处出现：**延迟加载需要复用成熟缓存语义，避免手写锁**。先不要套类图，先写下如果沿用原结构，需要改哪些判断、对象和测试。

这一轮真正的问题是：如何利用 Python 的缓存函数避免手写双重检查锁？ 最直接的选择仍然是继续修改原函数；第一次这样做通常最便宜。等相同方向的修改再次出现，我们才获得足够证据，知道应该把哪部分从主流程中分离。

### 这一轮只做一个设计动作

本轮只落实 **单例（四）：缓存函数的延迟初始化**：围绕“缓存函数、延迟初始化、生命周期、线程安全”移动一个边界，不同时引入后续模式。验证标准也很具体——完成本轮需求后，再增加同方向实现时，上一轮已经稳定的业务结果不需要改写。

完成动作后再给它命名：这一轮对应 **缓存函数、延迟初始化、生命周期、线程安全**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

这一轮也增加了一层命名和间接调用。下一轮需求会继续检验它；如果修改清单没有缩短，就退回更直接的版本，而不是继续叠抽象。

## 第 6 轮需求：先解决问题，最后再叫它 全局状态（P21）

我们继续修改同一个“需要延迟加载、测试替换和进程级复用的配置目录”，不另起一个玩具项目。上一轮代码能工作，但新的需求让这个薄弱处出现：**业务对象仍显式接收 Catalog，单例只属于组合根的生命周期决策**。先不要套类图，先写下如果沿用原结构，需要改哪些判断、对象和测试。

这一轮真正的问题是：什么时候需要一个实例，什么时候只是为了少传一个参数？ 最直接的选择仍然是继续修改原函数；第一次这样做通常最便宜。等相同方向的修改再次出现，我们才获得足够证据，知道应该把哪部分从主流程中分离。

### 这一轮只做一个设计动作

本轮只落实 **单例总结：唯一实例不等于全局可访问**：围绕“全局状态、作用域、可测试性、替代方案”移动一个边界，不同时引入后续模式。验证标准也很具体——完成本轮需求后，再增加同方向实现时，上一轮已经稳定的业务结果不需要改写。

完成动作后再给它命名：这一轮对应 **全局状态、作用域、可测试性、替代方案**。名称只是压缩沟通，不是推导起点。如果无法从“需求如何变化”推回这个结构，就说明我们只记住了答案。

这一轮也增加了一层命名和间接调用。下一轮需求会继续检验它；如果修改清单没有缩短，就退回更直接的版本，而不是继续叠抽象。


## 演进完成后的 Python 实现

下面不是另一个示例，而是起点代码承受完上述需求后的版本。每个新增角色都能追溯到前面某一轮真实修改压力；删掉任何一层，都应该能说出哪项需求会重新变难。

```python
from functools import lru_cache

class Catalog:
    def __init__(self, values: dict[str, str]) -> None: self.values = values
    def find(self, key: str) -> str | None: return self.values.get(key)

@lru_cache(maxsize=1)
def application_catalog() -> Catalog:
    return Catalog({"version": "1.0"})

class ShowVersion:
    def __init__(self, catalog: Catalog) -> None: self.catalog = catalog
    def run(self) -> str: return self.catalog.find("version") or "unknown"
```

![从模式语言到单例生命周期：唯一实例不等于全局变量：案例 UML 结构与对象关系](/images/notes/programming-thoughts/diagrams/chapter-patterns-and-singleton-lifecycle-uml.svg)

这张 UML 只画最终需要长期沟通的关系。虚线强调依赖或接口实现，菱形表示对象拥有或包装另一个对象；创建和调用细节仍以 Python 代码为准。

## 用真实业务结果验证，而不是测试模式名

测试不关心类图里有几个角色，只验证稳定业务结果、替换能力和关键失败边界。下面代码与上一个代码块拼接后可直接由 Python 3.12 执行。

```python
assert application_catalog() is application_catalog()
assert ShowVersion(application_catalog()).run() == "1.0"
fake = Catalog({"version": "test"})
assert ShowVersion(fake).run() == "test"
```

![从模式语言到单例生命周期：唯一实例不等于全局变量：同一份代码重构前后结构对照](/images/notes/programming-thoughts/diagrams/chapter-patterns-and-singleton-lifecycle-comparison.svg)

## 把连续重构放回一张决策表

| 检查项 | 仍然简单时 | 变化已经重复时 | 抽象后的验证 |
| --- | --- | --- | --- |
| 修改范围 | 一个分支或一个函数 | 同类判断散落到多个模块 | 新需求主要新增实现 |
| 依赖语言 | 具体类名与外部字段 | 业务规则被实现细节牵着走 | 调用方只看自己的业务能力 |
| 测试方式 | 端到端手工验证 | 每次都要启动整套环境 | 稳定规则可用替身局部测试 |
| 撤销成本 | 几乎没有 | 继续堆分支的成本持续上升 | 抽象仍能被一次小重构移除 |

这四行里最容易被忽略的是“撤销成本”。好的演进式设计不是层数最多，而是每一步都能解释、能验证、也能退回去。没有第二个真实用例时，保留直接实现通常更诚实；出现重复变化后仍拒绝命名边界，则会把复杂度转移给未来维护者。

## 为什么这些分 P 必须连在一起读

单独看每个名词，很容易把设计理解成一组互不相关的技巧。放回同一条演进链后会发现，前一步通常在制造后一步所需的条件：先把变化聚到局部，才看得见稳定边界；先让依赖显式，才谈得上替换；先定义替换契约，才有资格把实现交给工厂、注册表或组合根。

同样，后一种结构并不会淘汰前一种。一个系统完全可能在入口保留简单分支，在核心流程使用协议，在基础设施侧使用注册表。选择标准不是“哪个模式更高级”，而是当前变化由谁发起、发生频率多高、失败后需要谁负责。

可以用三次追问检查理解是否连贯。第一，去掉新抽象后，哪一个真实需求会重新变难？第二，新增一种实现时，哪些稳定代码仍然会被修改？第三，实现抛错、超时或返回边界值时，调用方依赖的行为是否仍然成立？如果答不出来，先回到上一个更简单的版本。

本文把代码拆成多个小切片，是为了让每次决策清楚，并不建议在项目中为每个概念各建一层。真正落地时，应该把相邻且同向变化的角色合并，把仅服务一个调用点的抽象留在模块内部，只把跨模块需要共同遵守的契约公开出去。这样既保留演进路径，也不会把教学结构原样搬成生产结构。

## 容易走偏的地方

1. **第一版就设计最终形态。** 这会跳过最重要的证据收集，也让团队无法解释每层抽象来自哪个需求。
2. **用类的数量衡量设计。** 类少不等于简单，类多也不等于解耦；真正要看的是一次变化传播多远。
3. **只画重构后的图。** 没有“之前为什么痛”，读者只能背结构，无法迁移到新的问题。
4. **把 Protocol 当作自动解耦。** 如果接口仍然复制供应商语言，业务只是隔着一层继续依赖供应商。
5. **忽略运行时与失败语义。** 能正常返回只是契约的一部分，超时、重复、并发和部分失败同样决定实现能否替换。

## 什么时候停在更简单的版本

如果实现只有一个、变化方向长期稳定、团队无法给出第二个真实需求，先停在函数或小类即可。本文的每次重构都有前提：旧结构已经让某种变化重复付费。没有这个前提，所谓扩展性很容易变成需要长期维护的猜测。

反过来，如果同一个条件分支已经复制三次、测试必须连接真实基础设施、或一个调用方需要穿过三层对象才能回答业务问题，就不要再用“以后再说”掩盖结构债务。此时先写修改清单，再选择本文最小的一步，而不是一次引入所有模式。

## 练习：把视频推导重新走一遍

为“需要延迟加载、测试替换和进程级复用的配置目录”增加一个与现有实现明显不同的需求。第一轮禁止增加接口，只修改朴素版本并记录触碰点；第二轮使用本文对应步骤重构；第三轮再加入一个变化，验证旧流程是否保持不动。

提交物包括：重构前后的修改清单、一张依赖方向图、至少两个业务测试、一个共享契约测试，以及一段不超过 150 字的代价说明。最后回看 P16–P21，确认每个结构都能追溯到一个真实失败，而不是追溯到一个想背下来的名词。

## 小结

本章真正形成的是一条推理链：直接实现 → 新需求暴露耦合 → 找到变化轴 → 放置最小边界 → 用测试证明 → 写下代价。做到这一步，模式语言、饿汉式、模块对象、构造守卫、缓存函数、全局状态 才不再是彼此分散的知识点，而是同一段代码演进过程中自然出现的工具。
